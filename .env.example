DATABASE_URL=postgres://vacu:vacu@localhost:5436/vacu
AUTH_SECRET=change-me-to-32-plus-random-bytes
ADMIN_EMAIL=admin@vacu.com.vn
ADMIN_PASSWORD=change-me-once-then-remove
UPLOADS_DIR=./public/uploads
# Trusted proxies in front of the app (for picking the real client IP from
# X-Forwarded-For when rate limiting). 1 = single ingress; raise if a CDN also fronts it.
TRUSTED_PROXY_HOPS=1
