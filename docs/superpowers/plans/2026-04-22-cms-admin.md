# CMS Admin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an admin CMS at `/admin/*` that manages all content (products, categories, farmers, testimonials, FAQ, site info, orders, users) backed by Postgres, replacing the current static JSON files while keeping the public site UI identical.

**Architecture:** Single Next.js 16 app. Route group `(public)` for the existing storefront, `/admin/*` for CMS. Postgres via Drizzle ORM. Session auth with JWT cookie + `proxy.ts`. Image uploads to local filesystem resized by `sharp`. Mutations through Server Actions.

**Tech Stack:** Next.js 16, React 19, TypeScript, Tailwind v4, Postgres 17, Drizzle ORM, `pg`, `jose` (JWT), `argon2`, `zod`, `sharp`, `tsx`, `vitest`, `testcontainers`.

**Reference spec:** `docs/superpowers/specs/2026-04-22-cms-admin-design.md`

---

## File Structure

```
vacu/
├── app/
│   ├── layout.tsx                       # ROOT: <html><body> + fonts
│   ├── (public)/
│   │   ├── layout.tsx                   # Navbar + Footer + CartProvider + CartDrawer
│   │   ├── page.tsx                     # home (moved from app/page.tsx)
│   │   ├── about/page.tsx               # moved
│   │   ├── products/                    # moved
│   │   ├── farmers/                     # moved
│   │   ├── contact/                     # moved
│   │   ├── checkout/                    # moved
│   │   └── orders/                      # moved, rewritten
│   ├── admin/
│   │   ├── layout.tsx
│   │   ├── login/page.tsx
│   │   ├── page.tsx                     # dashboard
│   │   ├── products/{page,new/page,[id]/edit/page}.tsx
│   │   ├── categories/{page,new/page,[id]/edit/page}.tsx
│   │   ├── farmers/{page,new/page,[id]/edit/page}.tsx
│   │   ├── testimonials/{page,new/page,[id]/edit/page}.tsx
│   │   ├── faq/{page,new/page,[id]/edit/page}.tsx
│   │   ├── orders/{page,[id]/page}.tsx
│   │   ├── users/{page,new/page,[id]/edit/page}.tsx
│   │   ├── settings/page.tsx
│   │   └── actions/                     # server actions per entity
│   │       ├── auth.ts
│   │       ├── products.ts
│   │       ├── categories.ts
│   │       ├── farmers.ts
│   │       ├── testimonials.ts
│   │       ├── faq.ts
│   │       ├── orders.ts
│   │       ├── users.ts
│   │       └── settings.ts
│   └── api/
│       ├── upload/route.ts
│       └── auth/logout/route.ts
├── components/
│   ├── admin/
│   │   ├── Sidebar.tsx
│   │   ├── Topbar.tsx
│   │   ├── DataTable.tsx
│   │   ├── Pagination.tsx
│   │   ├── SearchBar.tsx
│   │   ├── FormField.tsx
│   │   ├── SubmitButton.tsx
│   │   ├── DeleteButton.tsx
│   │   └── ImageUpload.tsx
│   └── …existing public components
├── db/
│   ├── schema.ts
│   ├── client.ts
│   ├── migrate.ts
│   ├── seed.ts
│   └── seed-admin.ts
├── drizzle/                             # generated
├── lib/
│   ├── data.ts                          # async query helpers
│   ├── auth.ts                          # hash, verify, sign/verify JWT
│   ├── session.ts                       # getSession, requireAdmin, requireRole
│   ├── uploads.ts                       # save+resize
│   ├── validators.ts                    # zod schemas
│   └── orders-cookie.ts                 # ntx_my_orders helpers
├── tests/
│   ├── lib/{auth,uploads,validators,orders-cookie}.test.ts
│   └── integration/{auth,products,orders,...}.test.ts
├── proxy.ts
├── drizzle.config.ts
├── docker-compose.yml
├── .env.example
└── public/uploads/                      # git-ignore
```

---

## Phase 0: Bootstrap

### Task 1: Install dependencies

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Install runtime deps**

Run:
```bash
npm install drizzle-orm pg jose @node-rs/argon2 zod sharp
```

- [ ] **Step 2: Install dev deps**

Run:
```bash
npm install -D drizzle-kit tsx @types/pg vitest testcontainers dotenv
```

- [ ] **Step 3: Add scripts**

Edit `package.json` scripts block:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx db/migrate.ts",
    "db:seed": "tsx db/seed.ts",
    "db:seed-admin": "tsx db/seed-admin.ts"
  }
}
```

- [ ] **Step 4: Commit**

```bash
git init
git add package.json package-lock.json
git commit -m "chore: add cms dependencies and scripts"
```

---

### Task 2: Docker Compose + env

**Files:**
- Create: `docker-compose.yml`
- Create: `.env.example`
- Create: `.env.local`
- Modify: `.gitignore`

- [ ] **Step 1: Write docker-compose.yml**

```yaml
services:
  postgres:
    image: postgres:17
    container_name: vacu-postgres
    environment:
      POSTGRES_USER: vacu
      POSTGRES_PASSWORD: vacu
      POSTGRES_DB: vacu
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vacu"]
      interval: 5s
      timeout: 5s
      retries: 5
volumes:
  postgres-data:
```

- [ ] **Step 2: Write .env.example**

```
DATABASE_URL=postgres://vacu:vacu@localhost:5432/vacu
AUTH_SECRET=change-me-to-32-plus-random-bytes
ADMIN_EMAIL=admin@nongtraixanh.vn
ADMIN_PASSWORD=change-me-once-then-remove
UPLOADS_DIR=./public/uploads
```

- [ ] **Step 3: Create real .env.local**

```bash
cp .env.example .env.local
# Generate a real secret
echo "AUTH_SECRET=$(openssl rand -hex 32)" >> .env.local.tmp
# User manually merges secret into .env.local before running
```

- [ ] **Step 4: Extend .gitignore**

Append to `.gitignore`:

```
.env*.local
/public/uploads
postgres-data/
```

- [ ] **Step 5: Start Postgres**

```bash
npm run db:up
docker compose ps
```
Expected: `vacu-postgres ... healthy`

- [ ] **Step 6: Commit**

```bash
git add docker-compose.yml .env.example .gitignore
git commit -m "chore: postgres docker-compose and env template"
```

---

## Phase 1: Database schema + client

### Task 3: Drizzle config + client

**Files:**
- Create: `drizzle.config.ts`
- Create: `db/client.ts`

- [ ] **Step 1: drizzle.config.ts**

```ts
import 'dotenv/config';
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
  strict: true,
  verbose: true,
});
```

- [ ] **Step 2: db/client.ts**

```ts
import 'dotenv/config';
import { Pool } from 'pg';
import { drizzle } from 'drizzle-orm/node-postgres';
import * as schema from './schema';

const globalForDb = globalThis as unknown as { pool?: Pool };

export const pool =
  globalForDb.pool ??
  new Pool({
    connectionString: process.env.DATABASE_URL,
    max: 10,
  });

if (process.env.NODE_ENV !== 'production') globalForDb.pool = pool;

export const db = drizzle(pool, { schema });
export type DB = typeof db;
```

- [ ] **Step 3: Commit**

```bash
git add drizzle.config.ts db/client.ts
git commit -m "feat(db): drizzle config and client"
```

---

### Task 4: Drizzle schema (all tables)

**Files:**
- Create: `db/schema.ts`

- [ ] **Step 1: Write schema**

```ts
import {
  pgTable, text, integer, serial, boolean, timestamp, jsonb, uuid, check
} from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';
import { relations } from 'drizzle-orm';

export const users = pgTable('users', {
  id: uuid('id').primaryKey().defaultRandom(),
  email: text('email').notNull().unique(),
  passwordHash: text('password_hash').notNull(),
  name: text('name').notNull(),
  role: text('role', { enum: ['admin', 'staff'] }).notNull().default('staff'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
});

export const categories = pgTable('categories', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  icon: text('icon').notNull(),
  description: text('description').notNull(),
  sortOrder: integer('sort_order').default(0).notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
});

export const farmers = pgTable('farmers', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  farm: text('farm').notNull(),
  location: text('location').notNull(),
  years: integer('years').notNull(),
  specialty: text('specialty').notNull(),
  avatar: text('avatar').notNull(),
  cover: text('cover').notNull(),
  story: text('story').notNull(),
  certifications: jsonb('certifications').$type<string[]>().notNull().default([]),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
});

export const products = pgTable('products', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  categoryId: text('category_id').notNull().references(() => categories.id, { onDelete: 'restrict' }),
  unit: text('unit').notNull(),
  price: integer('price').notNull(),
  oldPrice: integer('old_price'),
  image: text('image').notNull(),
  farmerId: text('farmer_id').references(() => farmers.id, { onDelete: 'set null' }),
  description: text('description').notNull(),
  tags: jsonb('tags').$type<string[]>().notNull().default([]),
  featured: boolean('featured').notNull().default(false),
  inStock: boolean('in_stock').notNull().default(true),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
});

export const testimonials = pgTable('testimonials', {
  id: serial('id').primaryKey(),
  name: text('name').notNull(),
  role: text('role').notNull(),
  avatar: text('avatar').notNull(),
  content: text('content').notNull(),
  sortOrder: integer('sort_order').default(0).notNull(),
});

export const faqItems = pgTable('faq_items', {
  id: serial('id').primaryKey(),
  question: text('question').notNull(),
  answer: text('answer').notNull(),
  sortOrder: integer('sort_order').default(0).notNull(),
});

export const siteInfo = pgTable(
  'site_info',
  {
    id: integer('id').primaryKey(),
    name: text('name').notNull(),
    shortName: text('short_name').notNull(),
    tagline: text('tagline').notNull(),
    description: text('description').notNull(),
    address: text('address').notNull(),
    phone: text('phone').notNull(),
    email: text('email').notNull(),
    hours: text('hours').notNull(),
    statFarmers: text('stat_farmers').notNull(),
    statProducts: text('stat_products').notNull(),
    statCustomers: text('stat_customers').notNull(),
    statYears: text('stat_years').notNull(),
  },
  (t) => ({
    onlyOne: check('site_info_only_one', sql`${t.id} = 1`),
  }),
);

export const orders = pgTable(
  'orders',
  {
    id: text('id').primaryKey(),
    customerName: text('customer_name').notNull(),
    phone: text('phone').notNull(),
    address: text('address').notNull(),
    deliverySlot: text('delivery_slot').notNull(),
    note: text('note'),
    total: integer('total').notNull(),
    status: text('status', {
      enum: ['pending', 'preparing', 'delivering', 'delivered', 'cancelled'],
    }).notNull().default('pending'),
    createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
    updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
  },
);

export const orderItems = pgTable('order_items', {
  id: serial('id').primaryKey(),
  orderId: text('order_id').notNull().references(() => orders.id, { onDelete: 'cascade' }),
  productId: text('product_id').notNull(),
  name: text('name').notNull(),
  price: integer('price').notNull(),
  qty: integer('qty').notNull(),
  unit: text('unit').notNull(),
  image: text('image').notNull(),
});

export const ordersRelations = relations(orders, ({ many }) => ({
  items: many(orderItems),
}));
export const orderItemsRelations = relations(orderItems, ({ one }) => ({
  order: one(orders, { fields: [orderItems.orderId], references: [orders.id] }),
}));
export const productsRelations = relations(products, ({ one }) => ({
  category: one(categories, { fields: [products.categoryId], references: [categories.id] }),
  farmer: one(farmers, { fields: [products.farmerId], references: [farmers.id] }),
}));

export type UserRow = typeof users.$inferSelect;
export type ProductRow = typeof products.$inferSelect;
export type CategoryRow = typeof categories.$inferSelect;
export type FarmerRow = typeof farmers.$inferSelect;
export type TestimonialRow = typeof testimonials.$inferSelect;
export type FaqRow = typeof faqItems.$inferSelect;
export type SiteInfoRow = typeof siteInfo.$inferSelect;
export type OrderRow = typeof orders.$inferSelect;
export type OrderItemRow = typeof orderItems.$inferSelect;
```

- [ ] **Step 2: Generate migration**

```bash
npm run db:generate
ls drizzle/
```
Expected: a `0000_*.sql` file and `meta/` directory.

- [ ] **Step 3: Commit**

```bash
git add db/schema.ts drizzle/
git commit -m "feat(db): initial schema and migration"
```

---

### Task 5: Migration runner

**Files:**
- Create: `db/migrate.ts`

- [ ] **Step 1: Write migrate.ts**

```ts
import 'dotenv/config';
import { migrate } from 'drizzle-orm/node-postgres/migrator';
import { db, pool } from './client';

async function main() {
  console.log('Running migrations...');
  await migrate(db, { migrationsFolder: './drizzle' });
  console.log('Done.');
  await pool.end();
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: Run migration**

```bash
npm run db:migrate
```
Expected: `Done.` and no error.

- [ ] **Step 3: Verify tables exist**

```bash
docker exec vacu-postgres psql -U vacu -d vacu -c '\dt'
```
Expected: users, categories, farmers, products, testimonials, faq_items, site_info, orders, order_items listed.

- [ ] **Step 4: Commit**

```bash
git add db/migrate.ts
git commit -m "feat(db): migration runner"
```

---

### Task 6: Seed JSON → DB

**Files:**
- Create: `db/seed.ts`

- [ ] **Step 1: Write seed.ts**

```ts
import 'dotenv/config';
import { db, pool } from './client';
import {
  categories, farmers, products, testimonials, faqItems, siteInfo,
} from './schema';
import categoriesJson from '@/data/categories.json';
import farmersJson from '@/data/farmers.json';
import productsJson from '@/data/products.json';
import testimonialsJson from '@/data/testimonials.json';
import faqJson from '@/data/faq.json';
import infoJson from '@/data/info.json';

async function main() {
  console.log('Seeding categories...');
  for (const c of categoriesJson as any[]) {
    await db.insert(categories).values({
      id: c.id, name: c.name, icon: c.icon, description: c.description,
    }).onConflictDoNothing();
  }

  console.log('Seeding farmers...');
  for (const f of farmersJson as any[]) {
    await db.insert(farmers).values({
      id: f.id, name: f.name, farm: f.farm, location: f.location,
      years: f.years, specialty: f.specialty, avatar: f.avatar, cover: f.cover,
      story: f.story, certifications: f.certifications ?? [],
    }).onConflictDoNothing();
  }

  console.log('Seeding products...');
  for (const p of productsJson as any[]) {
    await db.insert(products).values({
      id: p.id, name: p.name, categoryId: p.category, unit: p.unit,
      price: p.price, oldPrice: p.oldPrice ?? null, image: p.image,
      farmerId: p.farmerId ?? null, description: p.description,
      tags: p.tags ?? [], featured: !!p.featured, inStock: !!p.inStock,
    }).onConflictDoNothing();
  }

  console.log('Seeding testimonials...');
  for (const [i, t] of (testimonialsJson as any[]).entries()) {
    await db.insert(testimonials).values({
      name: t.name, role: t.role, avatar: t.avatar, content: t.content,
      sortOrder: i * 10,
    });
  }

  console.log('Seeding FAQ...');
  for (const [i, q] of (faqJson as any[]).entries()) {
    await db.insert(faqItems).values({
      question: q.q, answer: q.a, sortOrder: i * 10,
    });
  }

  console.log('Seeding site_info...');
  const info = infoJson as any;
  await db.insert(siteInfo).values({
    id: 1,
    name: info.name, shortName: info.shortName, tagline: info.tagline,
    description: info.description, address: info.address, phone: info.phone,
    email: info.email, hours: info.hours,
    statFarmers: info.stats.farmers, statProducts: info.stats.products,
    statCustomers: info.stats.customers, statYears: info.stats.years,
  }).onConflictDoNothing();

  console.log('Done.');
  await pool.end();
}

main().catch((err) => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Run seed**

```bash
npm run db:seed
```
Expected: "Done." and no error.

- [ ] **Step 3: Verify rows**

```bash
docker exec vacu-postgres psql -U vacu -d vacu -c 'SELECT count(*) FROM products;'
```
Expected: a number > 0 (should match number of entries in products.json).

- [ ] **Step 4: Commit**

```bash
git add db/seed.ts
git commit -m "feat(db): seed script from JSON"
```

---

## Phase 2: Auth primitives

### Task 7: Password + JWT helpers

**Files:**
- Create: `lib/auth.ts`
- Create: `tests/lib/auth.test.ts`
- Create: `vitest.config.ts`

- [ ] **Step 1: Write vitest config**

```ts
// vitest.config.ts
import { defineConfig } from 'vitest/config';
import path from 'node:path';

export default defineConfig({
  resolve: {
    alias: { '@': path.resolve(__dirname, '.') },
  },
  test: {
    environment: 'node',
    include: ['tests/**/*.test.ts'],
    setupFiles: [],
  },
});
```

- [ ] **Step 2: Write failing test**

`tests/lib/auth.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { hashPassword, verifyPassword, signSession, verifySession } from '@/lib/auth';

describe('auth', () => {
  it('hashes and verifies a password', async () => {
    const hash = await hashPassword('hunter2!');
    expect(hash).not.toBe('hunter2!');
    expect(await verifyPassword('hunter2!', hash)).toBe(true);
    expect(await verifyPassword('wrong', hash)).toBe(false);
  });

  it('signs and verifies a session token', async () => {
    process.env.AUTH_SECRET = 'a'.repeat(64);
    const jwt = await signSession({ sub: 'user-1', role: 'admin' });
    const payload = await verifySession(jwt);
    expect(payload.sub).toBe('user-1');
    expect(payload.role).toBe('admin');
  });

  it('rejects a token signed by a different secret', async () => {
    process.env.AUTH_SECRET = 'a'.repeat(64);
    const jwt = await signSession({ sub: 'user-1', role: 'admin' });
    process.env.AUTH_SECRET = 'b'.repeat(64);
    await expect(verifySession(jwt)).rejects.toThrow();
  });
});
```

- [ ] **Step 3: Run test, verify it fails**

```bash
npx vitest run tests/lib/auth.test.ts
```
Expected: FAIL — module `@/lib/auth` not found.

- [ ] **Step 4: Implement lib/auth.ts**

```ts
import { hash as argonHash, verify as argonVerify } from '@node-rs/argon2';
import { SignJWT, jwtVerify, type JWTPayload } from 'jose';

const ARGON_OPTS = { memoryCost: 19456, timeCost: 2, parallelism: 1 };

export async function hashPassword(plain: string): Promise<string> {
  return argonHash(plain, ARGON_OPTS);
}

export async function verifyPassword(plain: string, hash: string): Promise<boolean> {
  try {
    return await argonVerify(hash, plain);
  } catch {
    return false;
  }
}

export type SessionClaims = { sub: string; role: 'admin' | 'staff' };

function secret(): Uint8Array {
  const raw = process.env.AUTH_SECRET;
  if (!raw || raw.length < 32) {
    throw new Error('AUTH_SECRET must be set (>=32 chars)');
  }
  return new TextEncoder().encode(raw);
}

export async function signSession(claims: SessionClaims): Promise<string> {
  return new SignJWT({ ...claims } as JWTPayload)
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime('7d')
    .sign(secret());
}

export async function verifySession(jwt: string): Promise<SessionClaims & JWTPayload> {
  const { payload } = await jwtVerify(jwt, secret(), { algorithms: ['HS256'] });
  if (typeof payload.sub !== 'string' || (payload.role !== 'admin' && payload.role !== 'staff')) {
    throw new Error('invalid session');
  }
  return payload as SessionClaims & JWTPayload;
}
```

- [ ] **Step 5: Run test, verify pass**

```bash
npx vitest run tests/lib/auth.test.ts
```
Expected: PASS (3 tests).

- [ ] **Step 6: Commit**

```bash
git add lib/auth.ts tests/lib/auth.test.ts vitest.config.ts
git commit -m "feat(auth): password hashing and jwt session helpers"
```

---

### Task 8: Seed admin user

**Files:**
- Create: `db/seed-admin.ts`

- [ ] **Step 1: Write seed-admin.ts**

```ts
import 'dotenv/config';
import { db, pool } from './client';
import { users } from './schema';
import { eq } from 'drizzle-orm';
import { hashPassword } from '@/lib/auth';

async function main() {
  const email = process.env.ADMIN_EMAIL;
  const password = process.env.ADMIN_PASSWORD;
  if (!email || !password) throw new Error('ADMIN_EMAIL and ADMIN_PASSWORD must be set');

  const existing = await db.select().from(users).where(eq(users.email, email)).limit(1);
  if (existing.length) {
    console.log(`Admin ${email} already exists — skipping.`);
  } else {
    const passwordHash = await hashPassword(password);
    await db.insert(users).values({ email, passwordHash, name: 'Admin', role: 'admin' });
    console.log(`Created admin ${email}.`);
  }
  await pool.end();
}

main().catch((err) => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Run**

```bash
npm run db:seed-admin
```
Expected: `Created admin admin@nongtraixanh.vn.`

- [ ] **Step 3: Commit**

```bash
git add db/seed-admin.ts
git commit -m "feat(auth): seed-admin script"
```

---

### Task 9: Session helpers

**Files:**
- Create: `lib/session.ts`

- [ ] **Step 1: Write lib/session.ts**

```ts
import { cookies } from 'next/headers';
import { redirect } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { users, type UserRow } from '@/db/schema';
import { signSession, verifySession, type SessionClaims } from './auth';

export const SESSION_COOKIE = 'ntx_session';
const MAX_AGE_SECONDS = 7 * 24 * 3600;

export async function setSessionCookie(claims: SessionClaims) {
  const jwt = await signSession(claims);
  const store = await cookies();
  store.set(SESSION_COOKIE, jwt, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    path: '/',
    maxAge: MAX_AGE_SECONDS,
  });
}

export async function clearSessionCookie() {
  const store = await cookies();
  store.delete(SESSION_COOKIE);
}

export async function getSession(): Promise<SessionClaims | null> {
  const store = await cookies();
  const raw = store.get(SESSION_COOKIE)?.value;
  if (!raw) return null;
  try {
    return await verifySession(raw);
  } catch {
    return null;
  }
}

export async function getCurrentUser(): Promise<UserRow | null> {
  const s = await getSession();
  if (!s) return null;
  const rows = await db.select().from(users).where(eq(users.id, s.sub)).limit(1);
  return rows[0] ?? null;
}

export async function requireAdmin(): Promise<UserRow> {
  const u = await getCurrentUser();
  if (!u) redirect('/admin/login');
  return u;
}

export async function requireRole(role: 'admin'): Promise<UserRow> {
  const u = await requireAdmin();
  if (u.role !== role) redirect('/admin');
  return u;
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/session.ts
git commit -m "feat(auth): session cookie helpers"
```

---

### Task 10: Validators

**Files:**
- Create: `lib/validators.ts`

- [ ] **Step 1: Write lib/validators.ts**

```ts
import { z } from 'zod';

const slug = z.string().min(1).max(80).regex(/^[a-z0-9-]+$/, 'Slug kiểu: chữ thường, số, gạch ngang');
const url = z.string().min(1).max(500);

export const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
  next: z.string().optional(),
});

export const categorySchema = z.object({
  id: slug,
  name: z.string().min(1).max(120),
  icon: z.string().min(1).max(10),
  description: z.string().min(1).max(300),
  sortOrder: z.coerce.number().int().default(0),
});

export const farmerSchema = z.object({
  id: slug,
  name: z.string().min(1).max(120),
  farm: z.string().min(1).max(120),
  location: z.string().min(1).max(120),
  years: z.coerce.number().int().min(0).max(100),
  specialty: z.string().min(1).max(200),
  avatar: url,
  cover: url,
  story: z.string().min(1).max(2000),
  certifications: z.array(z.string()).default([]),
});

export const productSchema = z.object({
  id: slug,
  name: z.string().min(1).max(200),
  categoryId: slug,
  unit: z.string().min(1).max(60),
  price: z.coerce.number().int().positive(),
  oldPrice: z.coerce.number().int().positive().optional().nullable(),
  image: url,
  farmerId: slug.optional().nullable(),
  description: z.string().min(1).max(2000),
  tags: z.array(z.string()).default([]),
  featured: z.coerce.boolean().default(false),
  inStock: z.coerce.boolean().default(true),
});

export const testimonialSchema = z.object({
  id: z.coerce.number().int().optional(),
  name: z.string().min(1).max(120),
  role: z.string().min(1).max(120),
  avatar: url,
  content: z.string().min(1).max(1000),
  sortOrder: z.coerce.number().int().default(0),
});

export const faqSchema = z.object({
  id: z.coerce.number().int().optional(),
  question: z.string().min(1).max(300),
  answer: z.string().min(1).max(2000),
  sortOrder: z.coerce.number().int().default(0),
});

export const siteInfoSchema = z.object({
  name: z.string().min(1),
  shortName: z.string().min(1),
  tagline: z.string().min(1),
  description: z.string().min(1),
  address: z.string().min(1),
  phone: z.string().min(1),
  email: z.string().email(),
  hours: z.string().min(1),
  statFarmers: z.string().min(1),
  statProducts: z.string().min(1),
  statCustomers: z.string().min(1),
  statYears: z.string().min(1),
});

export const userCreateSchema = z.object({
  email: z.string().email(),
  name: z.string().min(1).max(120),
  role: z.enum(['admin', 'staff']),
  password: z.string().min(8),
});

export const userUpdateSchema = userCreateSchema.partial({ password: true });

export const orderStatusSchema = z.enum(['pending', 'preparing', 'delivering', 'delivered', 'cancelled']);

export const placeOrderSchema = z.object({
  customerName: z.string().min(1).max(120),
  phone: z.string().min(6).max(20),
  address: z.string().min(5).max(500),
  deliverySlot: z.string().min(1).max(80),
  note: z.string().max(500).optional(),
});
```

- [ ] **Step 2: Commit**

```bash
git add lib/validators.ts
git commit -m "feat(validators): zod schemas for all entities"
```

---

## Phase 3: Public site migration (JSON → DB reads)

### Task 11: Restructure app/ into (public) route group

**Files:**
- Move: `app/page.tsx` → `app/(public)/page.tsx`
- Move: `app/about/` → `app/(public)/about/`
- Move: `app/products/` → `app/(public)/products/`
- Move: `app/farmers/` → `app/(public)/farmers/`
- Move: `app/contact/` → `app/(public)/contact/`
- Move: `app/checkout/` → `app/(public)/checkout/`
- Move: `app/orders/` → `app/(public)/orders/`
- Modify: `app/layout.tsx` (split root vs public)
- Create: `app/(public)/layout.tsx`

- [ ] **Step 1: Move files**

```bash
mkdir -p app/\(public\)
git mv app/page.tsx app/\(public\)/page.tsx
git mv app/about app/\(public\)/about
git mv app/products app/\(public\)/products
git mv app/farmers app/\(public\)/farmers
git mv app/contact app/\(public\)/contact
git mv app/checkout app/\(public\)/checkout
git mv app/orders app/\(public\)/orders
```

- [ ] **Step 2: Rewrite root `app/layout.tsx`**

```tsx
import type { Metadata } from 'next';
import { Inter, Fraunces } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin', 'vietnamese'], variable: '--font-sans' });
const fraunces = Fraunces({ subsets: ['latin', 'vietnamese'], variable: '--font-display' });

export const metadata: Metadata = {
  title: 'Nông Trại Xanh',
  description: 'Rau sạch từ nông trại tới bữa cơm gia đình',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="vi" className={`${inter.variable} ${fraunces.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans text-green-950">{children}</body>
    </html>
  );
}
```

- [ ] **Step 3: Create `app/(public)/layout.tsx`**

```tsx
import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';
import { CartProvider } from '@/components/CartProvider';
import CartDrawer from '@/components/CartDrawer';
import { getSiteInfo } from '@/lib/data';

export async function generateMetadata() {
  const info = await getSiteInfo();
  return { title: `${info.name} — ${info.tagline}`, description: info.description };
}

export default async function PublicLayout({ children }: { children: React.ReactNode }) {
  const info = await getSiteInfo();
  return (
    <CartProvider>
      <Navbar info={info} />
      <main className="flex-1">{children}</main>
      <Footer info={info} />
      <CartDrawer />
    </CartProvider>
  );
}
```

- [ ] **Step 4: Verify nothing renders yet**

```bash
npm run dev
# open http://localhost:3000 — will break because lib/data.ts getSiteInfo doesn't exist yet
# that's expected; next task fixes it
```

- [ ] **Step 5: Commit**

```bash
git add app/
git commit -m "refactor: move storefront under (public) route group"
```

---

### Task 12: Rewrite lib/data.ts as async DB queries

**Files:**
- Rewrite: `lib/data.ts`

- [ ] **Step 1: Rewrite lib/data.ts**

```ts
import 'server-only';
import { eq, desc, asc, and } from 'drizzle-orm';
import { db } from '@/db/client';
import {
  products, categories, farmers, testimonials, faqItems, siteInfo,
  type ProductRow, type CategoryRow, type FarmerRow,
  type TestimonialRow, type FaqRow, type SiteInfoRow,
} from '@/db/schema';

export type Product = ProductRow;
export type Category = CategoryRow;
export type Farmer = FarmerRow;
export type Testimonial = TestimonialRow;
export type FAQItem = FaqRow;
export type SiteInfo = SiteInfoRow;

export async function getAllProducts() {
  return db.select().from(products).orderBy(asc(products.name));
}
export async function getProduct(id: string) {
  const rows = await db.select().from(products).where(eq(products.id, id)).limit(1);
  return rows[0] ?? null;
}
export async function getProductsByCategory(categoryId: string) {
  return db.select().from(products).where(eq(products.categoryId, categoryId)).orderBy(asc(products.name));
}
export async function getFeaturedProducts(limit = 8) {
  return db.select().from(products).where(eq(products.featured, true)).limit(limit);
}
export async function getAllCategories() {
  return db.select().from(categories).orderBy(asc(categories.sortOrder), asc(categories.name));
}
export async function getCategory(id: string) {
  const rows = await db.select().from(categories).where(eq(categories.id, id)).limit(1);
  return rows[0] ?? null;
}
export async function getAllFarmers() {
  return db.select().from(farmers).orderBy(asc(farmers.name));
}
export async function getFarmer(id: string | null) {
  if (!id) return null;
  const rows = await db.select().from(farmers).where(eq(farmers.id, id)).limit(1);
  return rows[0] ?? null;
}
export async function getProductsByFarmer(farmerId: string) {
  return db.select().from(products).where(eq(products.farmerId, farmerId)).orderBy(asc(products.name));
}
export async function getAllTestimonials() {
  return db.select().from(testimonials).orderBy(asc(testimonials.sortOrder), asc(testimonials.id));
}
export async function getAllFaqItems() {
  return db.select().from(faqItems).orderBy(asc(faqItems.sortOrder), asc(faqItems.id));
}
export async function getSiteInfo(): Promise<SiteInfoRow> {
  const rows = await db.select().from(siteInfo).where(eq(siteInfo.id, 1)).limit(1);
  if (!rows[0]) throw new Error('site_info row missing — run npm run db:seed');
  return rows[0];
}

export function formatPrice(v: number) {
  return new Intl.NumberFormat('vi-VN', {
    style: 'currency', currency: 'VND', maximumFractionDigits: 0,
  }).format(v);
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/data.ts
git commit -m "refactor(data): async DB-backed query helpers"
```

---

### Task 13: Update existing public pages to `await` data

**Files:**
- Modify: `app/(public)/page.tsx`
- Modify: `app/(public)/about/page.tsx`
- Modify: `app/(public)/products/page.tsx`
- Modify: `app/(public)/products/[id]/page.tsx`
- Modify: `app/(public)/farmers/page.tsx`
- Modify: `app/(public)/farmers/[id]/page.tsx`
- Modify: `app/(public)/contact/page.tsx`
- Modify: `app/(public)/checkout/page.tsx`

- [ ] **Step 1: Update `app/(public)/page.tsx` header**

Replace the import block and function signature:

```tsx
import Link from 'next/link';
import {
  getAllCategories, getFeaturedProducts, getAllFarmers,
  getAllTestimonials, getSiteInfo, getAllFaqItems,
} from '@/lib/data';
import ProductCard from '@/components/ProductCard';
import FarmerCard from '@/components/FarmerCard';
import AnimateOnScroll from '@/components/AnimateOnScroll';
import FAQ from '@/components/FAQ';

export default async function HomePage() {
  const [categories, featured, farmers, testimonials, info, faqItems] = await Promise.all([
    getAllCategories(), getFeaturedProducts(8), getAllFarmers(),
    getAllTestimonials(), getSiteInfo(), getAllFaqItems(),
  ]);
  const topFarmers = farmers.slice(0, 3);
  // ...rest of JSX unchanged (already references categories, featured, topFarmers, testimonials, info, faqItems)
}
```

- [ ] **Step 2: Update `app/(public)/about/page.tsx`**

```tsx
import { getSiteInfo } from '@/lib/data';

export default async function AboutPage() {
  const info = await getSiteInfo();
  // replace any `info.` references — they still work the same
  // (keep existing JSX, only update the data source)
}
```

Inspect current file; replace `import { info } from '@/lib/data'` with the pattern above.

- [ ] **Step 3: Update products listing**

`app/(public)/products/page.tsx`:

```tsx
import { getAllCategories, getAllProducts, getProductsByCategory } from '@/lib/data';
// Replace top-level `products` / `categories` arrays:

export default async function ProductsPage({ searchParams }: { searchParams: Promise<{ c?: string }> }) {
  const { c } = await searchParams;
  const [categories, products] = await Promise.all([
    getAllCategories(),
    c ? getProductsByCategory(c) : getAllProducts(),
  ]);
  // ...rest unchanged
}
```

- [ ] **Step 4: Update product detail**

`app/(public)/products/[id]/page.tsx`:

```tsx
import { notFound } from 'next/navigation';
import { getProduct, getFarmer, getAllProducts } from '@/lib/data';

export default async function ProductDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const product = await getProduct(id);
  if (!product) notFound();
  const [farmer, all] = await Promise.all([
    product.farmerId ? getFarmer(product.farmerId) : Promise.resolve(null),
    getAllProducts(),
  ]);
  const related = all.filter((p) => p.categoryId === product.categoryId && p.id !== product.id).slice(0, 4);
  // ...rest unchanged
}
```

- [ ] **Step 5: Update farmers pages**

`app/(public)/farmers/page.tsx`:

```tsx
import { getAllFarmers } from '@/lib/data';

export default async function FarmersPage() {
  const farmers = await getAllFarmers();
  // ...rest unchanged
}
```

`app/(public)/farmers/[id]/page.tsx`:

```tsx
import { notFound } from 'next/navigation';
import { getFarmer, getProductsByFarmer } from '@/lib/data';

export default async function FarmerDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const farmer = await getFarmer(id);
  if (!farmer) notFound();
  const products = await getProductsByFarmer(id);
  // ...rest unchanged
}
```

- [ ] **Step 6: Update contact + checkout**

`app/(public)/contact/page.tsx`:

```tsx
import { getSiteInfo } from '@/lib/data';
export default async function ContactPage() {
  const info = await getSiteInfo();
  // ...rest unchanged
}
```

`app/(public)/checkout/page.tsx`: if it only pulls `info`/`formatPrice`, do the same pattern.

- [ ] **Step 7: Run site and smoke all routes**

```bash
npm run dev
```
Then manually open: `/`, `/products`, `/products/rau-cai-xoan`, `/farmers`, `/farmers/nguyen-thi-hoa`, `/about`, `/contact`, `/checkout`. All must render without error.

- [ ] **Step 8: Commit**

```bash
git add app/\(public\)/
git commit -m "refactor(public): await DB-backed data in all pages"
```

---

### Task 14: Fix components that still import static data

**Files:**
- Modify: `components/Navbar.tsx`
- Modify: `components/Footer.tsx`
- Modify: `components/ProductCard.tsx` (only if it imports `info`/`formatPrice` — `formatPrice` still works)
- Modify: `components/FarmerCard.tsx`

- [ ] **Step 1: Navbar — accept info via prop**

Read current `components/Navbar.tsx`. Change the component signature and remove static `info` import:

```tsx
'use client';
import Link from 'next/link';
import type { SiteInfoRow } from '@/db/schema';
// ...existing imports

export default function Navbar({ info }: { info: SiteInfoRow }) {
  // replace any `info.` references from static import with the prop
  // ...existing JSX
}
```

Remove any `import { info } from '@/lib/data'`.

- [ ] **Step 2: Footer — same pattern**

```tsx
import type { SiteInfoRow } from '@/db/schema';

export default function Footer({ info }: { info: SiteInfoRow }) { /* ... */ }
```

- [ ] **Step 3: Product/Farmer cards — fix Product/Farmer types**

If they import `Product`/`Farmer` types from `lib/data`, those still exist but are now `ProductRow`/`FarmerRow`. The field names all match the spec (`id`, `name`, `categoryId` replaces `category`, `farmerId`, `oldPrice`, `inStock`, etc. — update any JSX references from `category` → `categoryId`).

- [ ] **Step 4: Build check**

```bash
npm run build
```
Expected: build succeeds.

- [ ] **Step 5: Commit**

```bash
git add components/
git commit -m "refactor: pass site info via props instead of static import"
```

---

## Phase 4: Orders refactor (localStorage → DB)

### Task 15: Orders cookie helper

**Files:**
- Create: `lib/orders-cookie.ts`
- Create: `tests/lib/orders-cookie.test.ts`

- [ ] **Step 1: Write failing test**

`tests/lib/orders-cookie.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { parseMyOrders, appendMyOrder } from '@/lib/orders-cookie';

describe('orders cookie', () => {
  it('parses empty/invalid cookies as empty list', () => {
    expect(parseMyOrders(undefined)).toEqual([]);
    expect(parseMyOrders('not-json')).toEqual([]);
    expect(parseMyOrders('{"not":"array"}')).toEqual([]);
  });
  it('parses valid JSON array of strings', () => {
    expect(parseMyOrders('["NTX-1","NTX-2"]')).toEqual(['NTX-1', 'NTX-2']);
  });
  it('prepends new order and caps at 20', () => {
    const existing = Array.from({ length: 20 }, (_, i) => `NTX-${i}`);
    const next = appendMyOrder(existing, 'NTX-NEW');
    expect(next[0]).toBe('NTX-NEW');
    expect(next).toHaveLength(20);
    expect(next).not.toContain('NTX-19');
  });
  it('deduplicates existing ids', () => {
    expect(appendMyOrder(['NTX-1', 'NTX-2'], 'NTX-1')).toEqual(['NTX-1', 'NTX-2']);
  });
});
```

- [ ] **Step 2: Run, verify fail**

```bash
npx vitest run tests/lib/orders-cookie.test.ts
```
Expected: FAIL — module not found.

- [ ] **Step 3: Implement**

```ts
// lib/orders-cookie.ts
export const MY_ORDERS_COOKIE = 'ntx_my_orders';
export const MY_ORDERS_MAX = 20;
export const MY_ORDERS_MAX_AGE = 30 * 24 * 3600;

export function parseMyOrders(raw: string | undefined): string[] {
  if (!raw) return [];
  try {
    const v = JSON.parse(raw);
    if (!Array.isArray(v)) return [];
    return v.filter((x) => typeof x === 'string');
  } catch {
    return [];
  }
}

export function appendMyOrder(existing: string[], id: string): string[] {
  if (existing.includes(id)) return existing;
  return [id, ...existing].slice(0, MY_ORDERS_MAX);
}
```

- [ ] **Step 4: Run, verify pass**

```bash
npx vitest run tests/lib/orders-cookie.test.ts
```
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/orders-cookie.ts tests/lib/orders-cookie.test.ts
git commit -m "feat(orders): cookie helpers for customer order lookup"
```

---

### Task 16: placeOrder server action

**Files:**
- Create: `app/(public)/checkout/actions.ts`
- Create: `tests/integration/place-order.test.ts`

- [ ] **Step 1: Write integration test**

`tests/integration/place-order.test.ts`:

```ts
import { beforeAll, afterAll, describe, it, expect } from 'vitest';
import { PostgreSqlContainer, type StartedPostgreSqlContainer } from '@testcontainers/postgresql';

let container: StartedPostgreSqlContainer;

beforeAll(async () => {
  container = await new PostgreSqlContainer('postgres:17').start();
  process.env.DATABASE_URL = container.getConnectionUri();
  const { migrate } = await import('drizzle-orm/node-postgres/migrator');
  const { db } = await import('@/db/client');
  await migrate(db, { migrationsFolder: './drizzle' });
}, 60_000);

afterAll(async () => {
  const { pool } = await import('@/db/client');
  await pool.end();
  await container.stop();
});

describe('placeOrder', () => {
  it('creates an order and items', async () => {
    const { db } = await import('@/db/client');
    const { categories, products, orders, orderItems } = await import('@/db/schema');
    await db.insert(categories).values({ id: 'c1', name: 'c1', icon: '🥬', description: '-' });
    await db.insert(products).values({
      id: 'p1', name: 'P1', categoryId: 'c1', unit: 'x', price: 10_000,
      image: '/x.jpg', description: '-',
    });

    const { placeOrder } = await import('@/app/(public)/checkout/actions');
    const fd = new FormData();
    fd.set('customerName', 'Ana');
    fd.set('phone', '0900000000');
    fd.set('address', '12 Đường Tre');
    fd.set('deliverySlot', '16:00-18:00');
    fd.set('cart', JSON.stringify([{ id: 'p1', name: 'P1', price: 10_000, qty: 2, unit: 'x', image: '/x.jpg' }]));

    const res = await placeOrder(fd);
    expect(res.ok).toBe(true);
    expect(res.orderId).toMatch(/^NTX-/);

    const { eq } = await import('drizzle-orm');
    const savedItems = await db.select().from(orderItems).where(eq(orderItems.orderId, res.orderId!));
    expect(savedItems).toHaveLength(1);
    expect(savedItems[0].qty).toBe(2);

    const savedOrder = await db.select().from(orders).where(eq(orders.id, res.orderId!)).limit(1);
    expect(savedOrder[0].total).toBe(20_000);
    expect(savedOrder[0].status).toBe('pending');
  });
});
```

- [ ] **Step 2: Run, verify fail**

```bash
npx vitest run tests/integration/place-order.test.ts
```
Expected: FAIL — module `@/app/(public)/checkout/actions` not found.

- [ ] **Step 3: Implement action**

```ts
// app/(public)/checkout/actions.ts
'use server';
import { cookies } from 'next/headers';
import { z } from 'zod';
import { db } from '@/db/client';
import { orders, orderItems } from '@/db/schema';
import { placeOrderSchema } from '@/lib/validators';
import {
  MY_ORDERS_COOKIE, MY_ORDERS_MAX_AGE,
  appendMyOrder, parseMyOrders,
} from '@/lib/orders-cookie';

const cartLineSchema = z.object({
  id: z.string(),
  name: z.string(),
  price: z.coerce.number().int().positive(),
  qty: z.coerce.number().int().positive(),
  unit: z.string(),
  image: z.string(),
});

export type PlaceOrderResult = { ok: true; orderId: string } | { ok: false; error: string };

function newOrderId(): string {
  return `NTX-${Date.now().toString().slice(-8)}`;
}

export async function placeOrder(formData: FormData): Promise<PlaceOrderResult> {
  const meta = placeOrderSchema.safeParse({
    customerName: formData.get('customerName'),
    phone: formData.get('phone'),
    address: formData.get('address'),
    deliverySlot: formData.get('deliverySlot'),
    note: formData.get('note') || undefined,
  });
  if (!meta.success) return { ok: false, error: 'Thông tin không hợp lệ' };

  let cart: z.infer<typeof cartLineSchema>[];
  try {
    const raw = formData.get('cart');
    cart = z.array(cartLineSchema).min(1).parse(JSON.parse(String(raw)));
  } catch {
    return { ok: false, error: 'Giỏ hàng trống hoặc không hợp lệ' };
  }

  const total = cart.reduce((s, i) => s + i.qty * i.price, 0);
  const orderId = newOrderId();

  await db.transaction(async (tx) => {
    await tx.insert(orders).values({ id: orderId, total, ...meta.data });
    await tx.insert(orderItems).values(
      cart.map((l) => ({
        orderId, productId: l.id, name: l.name, price: l.price,
        qty: l.qty, unit: l.unit, image: l.image,
      })),
    );
  });

  const store = await cookies();
  const existing = parseMyOrders(store.get(MY_ORDERS_COOKIE)?.value);
  store.set(MY_ORDERS_COOKIE, JSON.stringify(appendMyOrder(existing, orderId)), {
    httpOnly: false, path: '/', sameSite: 'lax', maxAge: MY_ORDERS_MAX_AGE,
  });

  return { ok: true, orderId };
}
```

- [ ] **Step 4: Run, verify pass**

```bash
npx vitest run tests/integration/place-order.test.ts
```
Expected: PASS (1 test).

- [ ] **Step 5: Commit**

```bash
git add app/\(public\)/checkout/actions.ts tests/integration/place-order.test.ts
git commit -m "feat(orders): placeOrder server action writes to DB"
```

---

### Task 17: Rewrite CheckoutForm

**Files:**
- Modify: `components/CheckoutForm.tsx`

- [ ] **Step 1: Update CheckoutForm**

Replace the submit handler in `components/CheckoutForm.tsx`:

```tsx
'use client';
import { useState, useTransition } from 'react';
import { useRouter } from 'next/navigation';
import { useCart } from './CartProvider';
import { placeOrder } from '@/app/(public)/checkout/actions';

export default function CheckoutForm() {
  const { items, total, clear } = useCart();
  const [error, setError] = useState<string | null>(null);
  const [pending, start] = useTransition();
  const router = useRouter();

  async function onSubmit(formData: FormData) {
    setError(null);
    formData.set('cart', JSON.stringify(items));
    start(async () => {
      const res = await placeOrder(formData);
      if (!res.ok) { setError(res.error); return; }
      clear();
      router.push(`/orders?new=${res.orderId}`);
    });
  }

  // keep existing JSX markup; change <form onSubmit> to <form action={onSubmit}>
  // keep existing fields with name="customerName", name="phone", etc.
  // show {error} above the submit button
  // disable submit when pending
}
```

- [ ] **Step 2: Smoke manually**

```bash
npm run dev
# Add to cart, go to /checkout, submit → should redirect to /orders?new=NTX-...
```

- [ ] **Step 3: Commit**

```bash
git add components/CheckoutForm.tsx
git commit -m "refactor(checkout): call placeOrder server action"
```

---

### Task 18: Rewrite /orders page + remove OrdersProvider

**Files:**
- Rewrite: `app/(public)/orders/page.tsx`
- Delete: `components/OrdersProvider.tsx`
- Modify: `app/(public)/layout.tsx` (already no OrdersProvider — verify)

- [ ] **Step 1: Rewrite orders page**

```tsx
// app/(public)/orders/page.tsx
import Link from 'next/link';
import { cookies } from 'next/headers';
import { inArray } from 'drizzle-orm';
import { db } from '@/db/client';
import { orders, orderItems } from '@/db/schema';
import { MY_ORDERS_COOKIE, parseMyOrders } from '@/lib/orders-cookie';
import { formatPrice } from '@/lib/data';

const STATUS_LABELS: Record<string, { label: string; color: string }> = {
  pending:    { label: 'Đã đặt',         color: 'bg-amber-100 text-amber-800' },
  preparing:  { label: 'Đang thu hoạch', color: 'bg-blue-100 text-blue-800' },
  delivering: { label: 'Đang giao',      color: 'bg-green-100 text-green-800' },
  delivered:  { label: 'Đã giao',        color: 'bg-stone-200 text-stone-700' },
  cancelled:  { label: 'Đã huỷ',         color: 'bg-red-100 text-red-700' },
};

export default async function OrdersPage({
  searchParams,
}: { searchParams: Promise<{ new?: string }> }) {
  const { new: newId } = await searchParams;
  const cookieStore = await cookies();
  const ids = parseMyOrders(cookieStore.get(MY_ORDERS_COOKIE)?.value);

  const myOrders = ids.length
    ? await db.select().from(orders).where(inArray(orders.id, ids))
    : [];
  myOrders.sort((a, b) => ids.indexOf(a.id) - ids.indexOf(b.id));

  const allItems = myOrders.length
    ? await db.select().from(orderItems).where(inArray(orderItems.orderId, myOrders.map((o) => o.id)))
    : [];
  const itemsByOrder = new Map<string, typeof allItems>();
  for (const it of allItems) {
    const arr = itemsByOrder.get(it.orderId) ?? [];
    arr.push(it); itemsByOrder.set(it.orderId, arr);
  }

  return (
    <div className="max-w-4xl mx-auto px-4 py-12 md:py-16">
      {newId && (
        <div className="bg-gradient-to-r from-green-700 to-green-800 text-white rounded-3xl p-8 mb-8 text-center">
          <div className="text-6xl mb-3">🌱</div>
          <h2 className="text-2xl md:text-3xl font-bold font-display mb-2">Đặt hàng thành công!</h2>
          <p className="text-green-100/90 mb-1">Mã đơn: <span className="font-bold">{newId}</span></p>
          <p className="text-green-100/80 text-sm">Cô nông dân sẽ thu hoạch rau của bạn vào sáng mai.</p>
        </div>
      )}
      <h1 className="text-3xl md:text-4xl font-bold text-green-950 font-display mb-2">Đơn hàng của tôi</h1>
      <p className="text-green-900/60 mb-8">Theo dõi rau từ vườn tới nhà</p>

      {myOrders.length === 0 ? (
        <div className="bg-white rounded-3xl border border-green-100 p-14 text-center">
          <div className="text-7xl mb-4">🧺</div>
          <h2 className="text-2xl font-bold text-green-950 mb-2 font-display">Chưa có đơn nào</h2>
          <p className="text-green-900/70 mb-6">Rau tươi đang chờ bạn trong vườn.</p>
          <Link href="/products" className="inline-block bg-green-700 hover:bg-green-800 text-white font-bold px-8 py-3 rounded-full">
            Đi chợ nông trại →
          </Link>
        </div>
      ) : (
        <div className="space-y-5">
          {myOrders.map((o) => {
            const s = STATUS_LABELS[o.status] ?? STATUS_LABELS.pending;
            const items = itemsByOrder.get(o.id) ?? [];
            return (
              <div key={o.id} className="bg-white rounded-3xl border border-green-100 p-6">
                <div className="flex flex-wrap items-start justify-between gap-3 mb-4">
                  <div>
                    <div className="text-xs text-green-900/60">Mã đơn</div>
                    <div className="font-bold text-green-950 text-lg">{o.id}</div>
                    <div className="text-xs text-green-900/60 mt-0.5">
                      {new Date(o.createdAt).toLocaleString('vi-VN')}
                    </div>
                  </div>
                  <span className={`text-xs font-bold px-3 py-1.5 rounded-full ${s.color}`}>{s.label}</span>
                </div>
                <ul className="space-y-2 mb-4 divide-y divide-green-50">
                  {items.map((it) => (
                    <li key={it.id} className="flex items-center gap-3 text-sm pt-2 first:pt-0">
                      {/* eslint-disable-next-line @next/next/no-img-element */}
                      <img src={it.image} alt={it.name} className="w-11 h-11 rounded-lg object-cover" />
                      <div className="flex-1 min-w-0">
                        <div className="text-green-950 font-semibold line-clamp-1">{it.name}</div>
                        <div className="text-xs text-green-900/60">{it.qty} × {it.unit}</div>
                      </div>
                      <div className="font-semibold text-green-800">{formatPrice(it.price * it.qty)}</div>
                    </li>
                  ))}
                </ul>
                <div className="border-t border-green-100 pt-4 grid md:grid-cols-3 gap-3 text-sm">
                  <div><div className="text-[11px] uppercase tracking-wider text-green-700 font-bold">Giao đến</div><div className="text-green-950">{o.address}</div></div>
                  <div><div className="text-[11px] uppercase tracking-wider text-green-700 font-bold">Khung giờ</div><div className="text-green-950">{o.deliverySlot}</div></div>
                  <div className="md:text-right"><div className="text-[11px] uppercase tracking-wider text-green-700 font-bold">Tổng tiền</div><div className="font-bold text-green-800 text-lg">{formatPrice(o.total)}</div></div>
                </div>
              </div>
            );
          })}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Delete OrdersProvider**

```bash
git rm components/OrdersProvider.tsx
```

If any file still imports it, remove the import. `app/(public)/layout.tsx` (from Task 11) should NOT wrap with it.

- [ ] **Step 3: Run site**

```bash
npm run dev
# Place an order via /checkout → land on /orders?new=NTX-XXX → see row.
```

- [ ] **Step 4: Commit**

```bash
git add app/\(public\)/orders components/
git commit -m "refactor(orders): read DB, drop localStorage provider"
```

---

## Phase 5: Upload pipeline

### Task 19: lib/uploads.ts

**Files:**
- Create: `lib/uploads.ts`
- Create: `tests/lib/uploads.test.ts`

- [ ] **Step 1: Failing test**

```ts
// tests/lib/uploads.test.ts
import { describe, it, expect, beforeAll } from 'vitest';
import { validateUpload, processImage } from '@/lib/uploads';
import { readFile } from 'node:fs/promises';
import path from 'node:path';
import sharp from 'sharp';

let pngBuffer: Buffer;

beforeAll(async () => {
  pngBuffer = await sharp({
    create: { width: 200, height: 150, channels: 3, background: { r: 0, g: 128, b: 0 } },
  }).png().toBuffer();
});

describe('uploads', () => {
  it('rejects wrong mime', () => {
    expect(() => validateUpload('application/pdf', 100)).toThrow(/mime/i);
  });
  it('rejects too large', () => {
    expect(() => validateUpload('image/png', 10 * 1024 * 1024)).toThrow(/lớn/i);
  });
  it('accepts good input', () => {
    expect(() => validateUpload('image/png', 1024)).not.toThrow();
  });
  it('processImage returns webp under 1200w', async () => {
    const out = await processImage(pngBuffer);
    const meta = await sharp(out).metadata();
    expect(meta.format).toBe('webp');
    expect(meta.width).toBeLessThanOrEqual(1200);
  });
});
```

- [ ] **Step 2: Run, verify fail**

```bash
npx vitest run tests/lib/uploads.test.ts
```
Expected: FAIL.

- [ ] **Step 3: Implement lib/uploads.ts**

```ts
import 'server-only';
import { mkdir, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { randomUUID } from 'node:crypto';
import sharp from 'sharp';

export const MAX_SIZE = 4 * 1024 * 1024;
export const ALLOWED = new Set(['image/jpeg', 'image/png', 'image/webp']);

export function validateUpload(mime: string, size: number) {
  if (!ALLOWED.has(mime)) throw new Error(`Mime không hỗ trợ: ${mime}`);
  if (size > MAX_SIZE) throw new Error('File quá lớn (>4MB)');
}

export async function processImage(buf: Buffer): Promise<Buffer> {
  return sharp(buf).rotate().resize({ width: 1200, withoutEnlargement: true }).webp({ quality: 82 }).toBuffer();
}

function uploadsDir(): string {
  return process.env.UPLOADS_DIR || './public/uploads';
}

export async function saveUpload(buf: Buffer): Promise<string> {
  const now = new Date();
  const yyyy = String(now.getFullYear());
  const mm = String(now.getMonth() + 1).padStart(2, '0');
  const dir = path.join(uploadsDir(), yyyy, mm);
  await mkdir(dir, { recursive: true });
  const filename = `${randomUUID()}.webp`;
  await writeFile(path.join(dir, filename), buf);
  return `/uploads/${yyyy}/${mm}/${filename}`;
}
```

- [ ] **Step 4: Run, verify pass**

```bash
npx vitest run tests/lib/uploads.test.ts
```
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/uploads.ts tests/lib/uploads.test.ts
git commit -m "feat(uploads): validate, resize and save images"
```

---

### Task 20: Upload route handler

**Files:**
- Create: `app/api/upload/route.ts`

- [ ] **Step 1: Write route**

```ts
// app/api/upload/route.ts
import { NextResponse } from 'next/server';
import { getCurrentUser } from '@/lib/session';
import { validateUpload, processImage, saveUpload } from '@/lib/uploads';

export async function POST(req: Request): Promise<Response> {
  const user = await getCurrentUser();
  if (!user) return NextResponse.json({ error: 'unauthorized' }, { status: 401 });

  const form = await req.formData();
  const file = form.get('file');
  if (!(file instanceof File)) return NextResponse.json({ error: 'Thiếu file' }, { status: 400 });

  try {
    validateUpload(file.type, file.size);
  } catch (e) {
    return NextResponse.json({ error: String((e as Error).message) }, { status: 400 });
  }

  const inBuf = Buffer.from(await file.arrayBuffer());
  const outBuf = await processImage(inBuf);
  const url = await saveUpload(outBuf);
  return NextResponse.json({ url });
}
```

- [ ] **Step 2: Commit**

```bash
git add app/api/upload/route.ts
git commit -m "feat(upload): POST /api/upload route"
```

---

### Task 21: ImageUpload component

**Files:**
- Create: `components/admin/ImageUpload.tsx`

- [ ] **Step 1: Write component**

```tsx
'use client';
import { useState } from 'react';

export default function ImageUpload({
  name, defaultValue, label,
}: { name: string; defaultValue?: string; label: string }) {
  const [url, setUrl] = useState(defaultValue ?? '');
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);

  async function upload(file: File) {
    setBusy(true); setError(null);
    try {
      const fd = new FormData();
      fd.set('file', file);
      const res = await fetch('/api/upload', { method: 'POST', body: fd });
      const json = await res.json();
      if (!res.ok) throw new Error(json.error || 'Upload lỗi');
      setUrl(json.url);
    } catch (e) {
      setError((e as Error).message);
    } finally {
      setBusy(false);
    }
  }

  return (
    <div className="space-y-2">
      <label className="text-sm font-medium text-green-950">{label}</label>
      <input type="hidden" name={name} value={url} />
      <div className="flex items-start gap-4">
        {url && (
          // eslint-disable-next-line @next/next/no-img-element
          <img src={url} alt="" className="w-24 h-24 rounded-lg object-cover border border-green-100" />
        )}
        <div className="flex-1">
          <input
            type="file"
            accept="image/jpeg,image/png,image/webp"
            disabled={busy}
            onChange={(e) => { const f = e.target.files?.[0]; if (f) upload(f); }}
            className="block text-sm"
          />
          <input
            type="text"
            value={url}
            onChange={(e) => setUrl(e.target.value)}
            placeholder="hoặc dán URL"
            className="mt-2 w-full border border-green-200 rounded px-3 py-1.5 text-sm"
          />
          {busy && <p className="text-xs text-green-700 mt-1">Đang xử lý…</p>}
          {error && <p className="text-xs text-red-600 mt-1">{error}</p>}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/admin/ImageUpload.tsx
git commit -m "feat(admin): image upload component"
```

---

## Phase 6: Admin shell + auth UI

### Task 22: proxy.ts

**Files:**
- Create: `proxy.ts`

- [ ] **Step 1: Write proxy.ts**

```ts
import { NextResponse, type NextRequest } from 'next/server';
import { jwtVerify } from 'jose';

const SECRET = new TextEncoder().encode(process.env.AUTH_SECRET || '');

async function isSessionValid(jwt: string | undefined): Promise<boolean> {
  if (!jwt || SECRET.length === 0) return false;
  try { await jwtVerify(jwt, SECRET, { algorithms: ['HS256'] }); return true; }
  catch { return false; }
}

export async function proxy(req: NextRequest) {
  const { pathname } = req.nextUrl;
  if (pathname === '/admin/login') return NextResponse.next();
  const jwt = req.cookies.get('ntx_session')?.value;
  if (await isSessionValid(jwt)) return NextResponse.next();
  const url = new URL('/admin/login', req.url);
  url.searchParams.set('next', pathname);
  return NextResponse.redirect(url);
}

export const config = {
  matcher: ['/admin/:path*'],
};
```

- [ ] **Step 2: Commit**

```bash
git add proxy.ts
git commit -m "feat(auth): proxy guards /admin/*"
```

---

### Task 23: Login page + action

**Files:**
- Create: `app/admin/login/page.tsx`
- Create: `app/admin/actions/auth.ts`

- [ ] **Step 1: signIn action**

```ts
// app/admin/actions/auth.ts
'use server';
import { redirect } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { users } from '@/db/schema';
import { verifyPassword } from '@/lib/auth';
import { setSessionCookie } from '@/lib/session';
import { loginSchema } from '@/lib/validators';

export type SignInState = { error?: string } | null;

export async function signIn(_prev: SignInState, formData: FormData): Promise<SignInState> {
  const parsed = loginSchema.safeParse({
    email: formData.get('email'),
    password: formData.get('password'),
    next: formData.get('next') ?? undefined,
  });
  if (!parsed.success) return { error: 'Email hoặc mật khẩu không hợp lệ.' };
  const { email, password, next } = parsed.data;

  const rows = await db.select().from(users).where(eq(users.email, email)).limit(1);
  const user = rows[0];
  if (!user) return { error: 'Sai email hoặc mật khẩu.' };

  const ok = await verifyPassword(password, user.passwordHash);
  if (!ok) return { error: 'Sai email hoặc mật khẩu.' };

  await setSessionCookie({ sub: user.id, role: user.role as 'admin' | 'staff' });
  redirect(next && next.startsWith('/admin') ? next : '/admin');
}
```

- [ ] **Step 2: Login page**

```tsx
// app/admin/login/page.tsx
'use client';
import { useActionState } from 'react';
import { useSearchParams } from 'next/navigation';
import { signIn, type SignInState } from '../actions/auth';

export default function LoginPage() {
  const params = useSearchParams();
  const next = params.get('next') ?? '/admin';
  const [state, action, pending] = useActionState<SignInState, FormData>(signIn, null);

  return (
    <div className="min-h-screen flex items-center justify-center bg-green-50/60 px-4">
      <form action={action} className="bg-white rounded-2xl shadow-sm border border-green-100 p-8 w-full max-w-md space-y-4">
        <h1 className="text-2xl font-bold font-display text-green-950">Đăng nhập quản trị</h1>
        <input type="hidden" name="next" value={next} />
        <label className="block">
          <span className="text-sm font-medium text-green-950">Email</span>
          <input name="email" type="email" required className="mt-1 w-full border border-green-200 rounded px-3 py-2" />
        </label>
        <label className="block">
          <span className="text-sm font-medium text-green-950">Mật khẩu</span>
          <input name="password" type="password" required className="mt-1 w-full border border-green-200 rounded px-3 py-2" />
        </label>
        {state?.error && <p className="text-sm text-red-600">{state.error}</p>}
        <button
          type="submit"
          disabled={pending}
          className="w-full bg-green-700 hover:bg-green-800 disabled:opacity-60 text-white font-bold py-2.5 rounded-full">
          {pending ? 'Đang đăng nhập…' : 'Đăng nhập'}
        </button>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add app/admin/login app/admin/actions/auth.ts
git commit -m "feat(admin): login page with server action"
```

---

### Task 24: Logout route + admin layout + sidebar

**Files:**
- Create: `app/api/auth/logout/route.ts`
- Create: `components/admin/Sidebar.tsx`
- Create: `components/admin/Topbar.tsx`
- Create: `app/admin/layout.tsx`

- [ ] **Step 1: Logout handler**

```ts
// app/api/auth/logout/route.ts
import { NextResponse } from 'next/server';
import { clearSessionCookie } from '@/lib/session';

export async function POST() {
  await clearSessionCookie();
  return NextResponse.redirect(new URL('/admin/login', 'http://placeholder'), { status: 303 });
}
```

Note: `NextResponse.redirect` requires an absolute URL; use `request.url` pattern:

```ts
import { NextResponse, type NextRequest } from 'next/server';
import { clearSessionCookie } from '@/lib/session';

export async function POST(req: NextRequest) {
  await clearSessionCookie();
  return NextResponse.redirect(new URL('/admin/login', req.url), { status: 303 });
}
```

- [ ] **Step 2: Sidebar component**

```tsx
// components/admin/Sidebar.tsx
import Link from 'next/link';

const LINKS = [
  { href: '/admin', label: 'Dashboard', icon: '🏠' },
  { href: '/admin/products', label: 'Sản phẩm', icon: '🥬' },
  { href: '/admin/categories', label: 'Danh mục', icon: '🏷️' },
  { href: '/admin/farmers', label: 'Nông dân', icon: '👨‍🌾' },
  { href: '/admin/testimonials', label: 'Cảm nhận', icon: '💬' },
  { href: '/admin/faq', label: 'FAQ', icon: '❓' },
  { href: '/admin/orders', label: 'Đơn hàng', icon: '📦' },
  { href: '/admin/users', label: 'Tài khoản', icon: '👤', adminOnly: true },
  { href: '/admin/settings', label: 'Cài đặt', icon: '⚙️' },
];

export default function Sidebar({ role }: { role: 'admin' | 'staff' }) {
  return (
    <nav className="w-60 bg-white border-r border-green-100 p-4 space-y-1">
      <div className="text-sm font-bold text-green-700 px-3 mb-2">Nông Trại Xanh CMS</div>
      {LINKS.filter((l) => !l.adminOnly || role === 'admin').map((l) => (
        <Link key={l.href} href={l.href} className="block px-3 py-2 rounded-lg hover:bg-green-50 text-green-950 text-sm">
          <span className="mr-2">{l.icon}</span>{l.label}
        </Link>
      ))}
    </nav>
  );
}
```

- [ ] **Step 3: Topbar component**

```tsx
// components/admin/Topbar.tsx
export default function Topbar({ email }: { email: string }) {
  return (
    <header className="h-14 border-b border-green-100 bg-white flex items-center justify-between px-6">
      <div className="text-sm text-green-900/70">Xin chào, {email}</div>
      <form action="/api/auth/logout" method="post">
        <button type="submit" className="text-sm text-green-700 font-semibold hover:underline">Đăng xuất</button>
      </form>
    </header>
  );
}
```

- [ ] **Step 4: Admin layout**

```tsx
// app/admin/layout.tsx
import { requireAdmin } from '@/lib/session';
import Sidebar from '@/components/admin/Sidebar';
import Topbar from '@/components/admin/Topbar';

export default async function AdminLayout({ children }: { children: React.ReactNode }) {
  const user = await requireAdmin();
  return (
    <div className="min-h-screen flex bg-green-50/40">
      <Sidebar role={user.role as 'admin' | 'staff'} />
      <div className="flex-1 flex flex-col">
        <Topbar email={user.email} />
        <div className="flex-1 p-6 overflow-auto">{children}</div>
      </div>
    </div>
  );
}
```

Note: the `login/page.tsx` must opt out of this layout. Because login lives under `app/admin/login/page.tsx`, it will inherit this layout and call `requireAdmin()` — creating a redirect loop.

**Fix:** put the login page in a route group `(auth)`:

```bash
mkdir -p "app/admin/(auth)"
git mv app/admin/login "app/admin/(auth)/login"
```

Then create `app/admin/(auth)/layout.tsx`:

```tsx
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

This route group layout overrides the parent — login will not call `requireAdmin()`. Update `proxy.ts` matcher exception: the current `pathname === '/admin/login'` check still works because route groups don't change URL.

- [ ] **Step 5: Smoke**

```bash
npm run dev
# go to /admin → redirected to /admin/login?next=/admin
# login → land on /admin (blank page OK, dashboard comes later)
# click Đăng xuất → back to /admin/login
```

- [ ] **Step 6: Commit**

```bash
git add app/admin components/admin/Sidebar.tsx components/admin/Topbar.tsx app/api/auth/logout
git commit -m "feat(admin): shell layout, sidebar, topbar, logout"
```

---

## Phase 7: Admin CRUD primitives

### Task 25: Shared form primitives

**Files:**
- Create: `components/admin/FormField.tsx`
- Create: `components/admin/SubmitButton.tsx`
- Create: `components/admin/DeleteButton.tsx`
- Create: `components/admin/DataTable.tsx`
- Create: `components/admin/Pagination.tsx`
- Create: `components/admin/SearchBar.tsx`

- [ ] **Step 1: FormField**

```tsx
// components/admin/FormField.tsx
import type { ReactNode } from 'react';

export default function FormField({
  label, name, error, hint, children,
}: { label: string; name?: string; error?: string[]; hint?: string; children: ReactNode }) {
  return (
    <div className="space-y-1.5">
      <label htmlFor={name} className="block text-sm font-medium text-green-950">{label}</label>
      {children}
      {hint && <p className="text-xs text-green-900/60">{hint}</p>}
      {error?.map((e, i) => <p key={i} className="text-xs text-red-600">{e}</p>)}
    </div>
  );
}
```

- [ ] **Step 2: SubmitButton**

```tsx
// components/admin/SubmitButton.tsx
'use client';
import { useFormStatus } from 'react-dom';

export default function SubmitButton({ children }: { children: React.ReactNode }) {
  const { pending } = useFormStatus();
  return (
    <button type="submit" disabled={pending}
      className="bg-green-700 hover:bg-green-800 disabled:opacity-60 text-white font-bold px-5 py-2 rounded-full text-sm">
      {pending ? 'Đang lưu…' : children}
    </button>
  );
}
```

- [ ] **Step 3: DeleteButton**

```tsx
// components/admin/DeleteButton.tsx
'use client';
import { useState, useTransition } from 'react';

export default function DeleteButton({ action, confirmText = 'Xoá mục này?' }: {
  action: () => Promise<{ ok: boolean; error?: string }>;
  confirmText?: string;
}) {
  const [busy, startTransition] = useTransition();
  const [err, setErr] = useState<string | null>(null);
  return (
    <form
      onSubmit={(e) => {
        e.preventDefault();
        if (!window.confirm(confirmText)) return;
        startTransition(async () => {
          setErr(null);
          const r = await action();
          if (!r.ok) setErr(r.error ?? 'Lỗi xoá');
        });
      }}>
      <button type="submit" disabled={busy} className="text-red-600 text-sm hover:underline disabled:opacity-60">
        {busy ? 'Đang xoá…' : 'Xoá'}
      </button>
      {err && <span className="text-xs text-red-600 ml-2">{err}</span>}
    </form>
  );
}
```

- [ ] **Step 4: DataTable**

```tsx
// components/admin/DataTable.tsx
import type { ReactNode } from 'react';

export type Column<T> = { header: string; cell: (row: T) => ReactNode; className?: string };

export default function DataTable<T>({ rows, columns, empty }: {
  rows: T[]; columns: Column<T>[]; empty?: string;
}) {
  if (rows.length === 0) {
    return <div className="bg-white rounded-xl border border-green-100 p-10 text-center text-green-900/70">{empty ?? 'Chưa có dữ liệu'}</div>;
  }
  return (
    <div className="bg-white rounded-xl border border-green-100 overflow-x-auto">
      <table className="w-full text-sm">
        <thead className="bg-green-50 text-green-900">
          <tr>{columns.map((c, i) => <th key={i} className={`text-left font-semibold px-4 py-2 ${c.className ?? ''}`}>{c.header}</th>)}</tr>
        </thead>
        <tbody className="divide-y divide-green-50">
          {rows.map((r, i) => (
            <tr key={i}>{columns.map((c, j) => <td key={j} className={`px-4 py-2 ${c.className ?? ''}`}>{c.cell(r)}</td>)}</tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 5: Pagination**

```tsx
// components/admin/Pagination.tsx
import Link from 'next/link';

export default function Pagination({ page, total, pageSize, params }: {
  page: number; total: number; pageSize: number; params: Record<string, string | undefined>;
}) {
  const pages = Math.max(1, Math.ceil(total / pageSize));
  function qs(p: number) {
    const s = new URLSearchParams();
    for (const [k, v] of Object.entries(params)) if (v) s.set(k, v);
    s.set('page', String(p));
    return `?${s.toString()}`;
  }
  return (
    <div className="flex gap-2 text-sm">
      {page > 1 && <Link href={qs(page - 1)} className="px-3 py-1 rounded border border-green-200">‹ Trước</Link>}
      <span className="px-3 py-1">Trang {page} / {pages}</span>
      {page < pages && <Link href={qs(page + 1)} className="px-3 py-1 rounded border border-green-200">Sau ›</Link>}
    </div>
  );
}
```

- [ ] **Step 6: SearchBar**

```tsx
// components/admin/SearchBar.tsx
export default function SearchBar({ defaultValue, placeholder }: { defaultValue?: string; placeholder?: string }) {
  return (
    <form className="flex gap-2" action="" method="get">
      <input type="text" name="q" defaultValue={defaultValue} placeholder={placeholder ?? 'Tìm kiếm…'} className="border border-green-200 rounded px-3 py-1.5 text-sm w-64" />
      <button className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">Tìm</button>
    </form>
  );
}
```

- [ ] **Step 7: Commit**

```bash
git add components/admin/
git commit -m "feat(admin): shared form and table primitives"
```

---

### Task 26: Dashboard

**Files:**
- Create: `app/admin/page.tsx`

- [ ] **Step 1: Dashboard page**

```tsx
// app/admin/page.tsx
import { desc, eq, sql } from 'drizzle-orm';
import { db } from '@/db/client';
import { orders, products } from '@/db/schema';
import { formatPrice } from '@/lib/data';

export default async function Dashboard() {
  const [todayRows, week, outOfStock, pending, recent] = await Promise.all([
    db.execute<{ count: number }>(sql`SELECT count(*)::int AS count FROM orders WHERE created_at::date = now()::date`),
    db.execute<{ total: number }>(sql`SELECT coalesce(sum(total),0)::int AS total FROM orders WHERE created_at > now() - interval '7 days'`),
    db.select({ n: sql<number>`count(*)::int` }).from(products).where(eq(products.inStock, false)),
    db.select({ n: sql<number>`count(*)::int` }).from(orders).where(eq(orders.status, 'pending')),
    db.select().from(orders).orderBy(desc(orders.createdAt)).limit(10),
  ]);

  const today = todayRows.rows[0]?.count ?? 0;
  const weekTotal = week.rows[0]?.total ?? 0;

  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold font-display text-green-950">Dashboard</h1>
      <div className="grid md:grid-cols-4 gap-4">
        <Card title="Đơn hôm nay" value={String(today)} />
        <Card title="Doanh thu 7 ngày" value={formatPrice(weekTotal)} />
        <Card title="Hết hàng" value={String(outOfStock[0]?.n ?? 0)} />
        <Card title="Đơn pending" value={String(pending[0]?.n ?? 0)} />
      </div>
      <div className="bg-white rounded-xl border border-green-100">
        <div className="p-4 border-b border-green-100 font-semibold">Đơn gần nhất</div>
        <ul className="divide-y divide-green-50">
          {recent.map((o) => (
            <li key={o.id} className="flex items-center justify-between px-4 py-3 text-sm">
              <a href={`/admin/orders/${o.id}`} className="text-green-800 font-semibold">{o.id}</a>
              <span>{o.customerName}</span>
              <span>{formatPrice(o.total)}</span>
              <span className="text-green-900/70">{new Date(o.createdAt).toLocaleString('vi-VN')}</span>
            </li>
          ))}
        </ul>
      </div>
    </div>
  );
}

function Card({ title, value }: { title: string; value: string }) {
  return (
    <div className="bg-white rounded-xl border border-green-100 p-5">
      <div className="text-xs text-green-900/70 uppercase">{title}</div>
      <div className="text-2xl font-bold font-display text-green-950">{value}</div>
    </div>
  );
}
```

- [ ] **Step 2: Smoke**

```bash
npm run dev
# /admin → dashboard cards render
```

- [ ] **Step 3: Commit**

```bash
git add app/admin/page.tsx
git commit -m "feat(admin): dashboard"
```

---

### Task 27: Products — actions + integration test

**Files:**
- Create: `app/admin/actions/products.ts`
- Create: `tests/integration/products-crud.test.ts`

- [ ] **Step 1: Failing integration test**

```ts
// tests/integration/products-crud.test.ts
import { beforeAll, afterAll, describe, it, expect } from 'vitest';
import { PostgreSqlContainer, type StartedPostgreSqlContainer } from '@testcontainers/postgresql';

let container: StartedPostgreSqlContainer;
beforeAll(async () => {
  container = await new PostgreSqlContainer('postgres:17').start();
  process.env.DATABASE_URL = container.getConnectionUri();
  process.env.AUTH_SECRET = 'a'.repeat(64);
  const { migrate } = await import('drizzle-orm/node-postgres/migrator');
  const { db } = await import('@/db/client');
  await migrate(db, { migrationsFolder: './drizzle' });
  const { categories } = await import('@/db/schema');
  await db.insert(categories).values({ id: 'c1', name: 'C1', icon: '🥬', description: '-' });
}, 60_000);

afterAll(async () => {
  const { pool } = await import('@/db/client');
  await pool.end();
  await container.stop();
});

describe('products CRUD actions', () => {
  it('creates, updates, and deletes a product', async () => {
    const { upsertProduct, deleteProduct } = await import('@/app/admin/actions/products');
    const fd = new FormData();
    fd.set('id', 'p-test');
    fd.set('name', 'P test');
    fd.set('categoryId', 'c1');
    fd.set('unit', 'bó');
    fd.set('price', '25000');
    fd.set('image', '/x.jpg');
    fd.set('description', 'desc');

    const res = await upsertProduct(null, fd);
    expect(res?.ok).toBe(true);

    const { db } = await import('@/db/client');
    const { products } = await import('@/db/schema');
    const { eq } = await import('drizzle-orm');
    const rows = await db.select().from(products).where(eq(products.id, 'p-test'));
    expect(rows[0].name).toBe('P test');

    fd.set('name', 'P updated');
    await upsertProduct(null, fd);
    const rows2 = await db.select().from(products).where(eq(products.id, 'p-test'));
    expect(rows2[0].name).toBe('P updated');

    await deleteProduct('p-test');
    const rows3 = await db.select().from(products).where(eq(products.id, 'p-test'));
    expect(rows3).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Run, verify fail**

```bash
npx vitest run tests/integration/products-crud.test.ts
```
Expected: FAIL — module not found.

- [ ] **Step 3: Implement action**

```ts
// app/admin/actions/products.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { products } from '@/db/schema';
import { productSchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';

export type FormResult =
  | { ok: true }
  | { ok: false; error?: string; fieldErrors?: Record<string, string[]> }
  | null;

export async function upsertProduct(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireAdmin();
  const raw = Object.fromEntries(formData.entries());
  const tagsRaw = String(raw.tags ?? '').trim();
  const data = {
    ...raw,
    tags: tagsRaw ? tagsRaw.split(',').map((s) => s.trim()).filter(Boolean) : [],
    featured: raw.featured === 'on' || raw.featured === 'true',
    inStock: raw.inStock !== 'false',
    oldPrice: raw.oldPrice ? Number(raw.oldPrice) : null,
    farmerId: raw.farmerId ? String(raw.farmerId) : null,
  };
  const parsed = productSchema.safeParse(data);
  if (!parsed.success) {
    return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  }
  const p = parsed.data;
  await db
    .insert(products)
    .values({ ...p, updatedAt: new Date() })
    .onConflictDoUpdate({
      target: products.id,
      set: {
        name: p.name, categoryId: p.categoryId, unit: p.unit,
        price: p.price, oldPrice: p.oldPrice ?? null, image: p.image,
        farmerId: p.farmerId ?? null, description: p.description,
        tags: p.tags, featured: p.featured, inStock: p.inStock,
        updatedAt: new Date(),
      },
    });
  revalidatePath('/');
  revalidatePath('/products');
  revalidatePath(`/products/${p.id}`);
  if (p.farmerId) revalidatePath(`/farmers/${p.farmerId}`);
  return { ok: true };
}

export async function deleteProduct(id: string): Promise<{ ok: boolean; error?: string }> {
  await requireAdmin();
  await db.delete(products).where(eq(products.id, id));
  revalidatePath('/');
  revalidatePath('/products');
  return { ok: true };
}
```

- [ ] **Step 4: Run test, verify pass**

```bash
npx vitest run tests/integration/products-crud.test.ts
```
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/admin/actions/products.ts tests/integration/products-crud.test.ts
git commit -m "feat(admin): products CRUD actions"
```

---

### Task 28: Products — UI pages

**Files:**
- Create: `app/admin/products/page.tsx`
- Create: `app/admin/products/new/page.tsx`
- Create: `app/admin/products/[id]/edit/page.tsx`
- Create: `components/admin/ProductForm.tsx`

- [ ] **Step 1: ProductForm component**

```tsx
// components/admin/ProductForm.tsx
'use client';
import { useActionState } from 'react';
import FormField from './FormField';
import SubmitButton from './SubmitButton';
import ImageUpload from './ImageUpload';
import { upsertProduct, type FormResult } from '@/app/admin/actions/products';
import type { CategoryRow, FarmerRow, ProductRow } from '@/db/schema';

export default function ProductForm({
  initial, categories, farmers,
}: { initial?: ProductRow; categories: CategoryRow[]; farmers: FarmerRow[] }) {
  const [state, action] = useActionState<FormResult, FormData>(upsertProduct, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  return (
    <form action={action} className="grid md:grid-cols-2 gap-6 bg-white rounded-xl border border-green-100 p-6">
      <div className="space-y-4">
        <FormField label="ID (slug)" error={e.id}>
          <input name="id" defaultValue={initial?.id} required readOnly={!!initial}
            className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
        </FormField>
        <FormField label="Tên sản phẩm" error={e.name}>
          <input name="name" defaultValue={initial?.name} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
        </FormField>
        <FormField label="Danh mục" error={e.categoryId}>
          <select name="categoryId" defaultValue={initial?.categoryId} required className="w-full border border-green-200 rounded px-3 py-2 text-sm">
            {categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}
          </select>
        </FormField>
        <FormField label="Nông dân" error={e.farmerId}>
          <select name="farmerId" defaultValue={initial?.farmerId ?? ''} className="w-full border border-green-200 rounded px-3 py-2 text-sm">
            <option value="">— Không chọn —</option>
            {farmers.map((f) => <option key={f.id} value={f.id}>{f.name}</option>)}
          </select>
        </FormField>
        <FormField label="Đơn vị (ví dụ: bó 250g)" error={e.unit}>
          <input name="unit" defaultValue={initial?.unit} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
        </FormField>
        <div className="grid grid-cols-2 gap-4">
          <FormField label="Giá (VND)" error={e.price}>
            <input name="price" type="number" defaultValue={initial?.price} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
          </FormField>
          <FormField label="Giá cũ (không bắt buộc)" error={e.oldPrice}>
            <input name="oldPrice" type="number" defaultValue={initial?.oldPrice ?? ''} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
          </FormField>
        </div>
      </div>
      <div className="space-y-4">
        <ImageUpload name="image" defaultValue={initial?.image} label="Ảnh sản phẩm" />
        <FormField label="Mô tả" error={e.description}>
          <textarea name="description" defaultValue={initial?.description} required rows={4} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
        </FormField>
        <FormField label="Tags (phẩy ngăn cách)" hint="ví dụ: Hữu cơ, VietGAP">
          <input name="tags" defaultValue={initial?.tags?.join(', ')} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
        </FormField>
        <div className="flex gap-6">
          <label className="flex items-center gap-2 text-sm">
            <input type="checkbox" name="featured" defaultChecked={initial?.featured} /> Nổi bật
          </label>
          <label className="flex items-center gap-2 text-sm">
            <input type="checkbox" name="inStock" value="true" defaultChecked={initial?.inStock ?? true} /> Còn hàng
          </label>
        </div>
        {state && !state.ok && state.error && <p className="text-sm text-red-600">{state.error}</p>}
        {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
        <SubmitButton>Lưu</SubmitButton>
      </div>
    </form>
  );
}
```

- [ ] **Step 2: Products list page**

```tsx
// app/admin/products/page.tsx
import Link from 'next/link';
import { ilike, or, desc, asc, sql } from 'drizzle-orm';
import { db } from '@/db/client';
import { products } from '@/db/schema';
import SearchBar from '@/components/admin/SearchBar';
import DataTable, { type Column } from '@/components/admin/DataTable';
import Pagination from '@/components/admin/Pagination';
import DeleteButton from '@/components/admin/DeleteButton';
import { deleteProduct } from '@/app/admin/actions/products';
import { formatPrice } from '@/lib/data';
import type { ProductRow } from '@/db/schema';

const PAGE_SIZE = 20;

export default async function AdminProducts({
  searchParams,
}: { searchParams: Promise<{ q?: string; page?: string }> }) {
  const { q, page: pageParam } = await searchParams;
  const page = Math.max(1, Number(pageParam) || 1);
  const where = q ? or(ilike(products.name, `%${q}%`), ilike(products.id, `%${q}%`)) : undefined;
  const [rows, totalRows] = await Promise.all([
    db.select().from(products).where(where as any).orderBy(asc(products.name)).limit(PAGE_SIZE).offset((page - 1) * PAGE_SIZE),
    db.select({ n: sql<number>`count(*)::int` }).from(products).where(where as any),
  ]);
  const total = totalRows[0]?.n ?? 0;

  const columns: Column<ProductRow>[] = [
    { header: 'Tên', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/products/${r.id}/edit`}>{r.name}</Link> },
    { header: 'Giá', cell: (r) => formatPrice(r.price) },
    { header: 'Danh mục', cell: (r) => r.categoryId },
    { header: 'Featured', cell: (r) => (r.featured ? '✓' : '') },
    { header: 'Kho', cell: (r) => (r.inStock ? '✓' : '—') },
    { header: '', cell: (r) => <DeleteButton action={async () => { 'use server'; return deleteProduct(r.id); }} /> },
  ];

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold font-display text-green-950">Sản phẩm</h1>
        <Link href="/admin/products/new" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">+ Sản phẩm mới</Link>
      </div>
      <SearchBar defaultValue={q} placeholder="Tìm theo tên / id…" />
      <DataTable rows={rows} columns={columns} empty="Chưa có sản phẩm" />
      <Pagination page={page} total={total} pageSize={PAGE_SIZE} params={{ q }} />
    </div>
  );
}
```

- [ ] **Step 3: New page**

```tsx
// app/admin/products/new/page.tsx
import { getAllCategories, getAllFarmers } from '@/lib/data';
import ProductForm from '@/components/admin/ProductForm';

export default async function NewProductPage() {
  const [categories, farmers] = await Promise.all([getAllCategories(), getAllFarmers()]);
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold font-display text-green-950">Sản phẩm mới</h1>
      <ProductForm categories={categories} farmers={farmers} />
    </div>
  );
}
```

- [ ] **Step 4: Edit page**

```tsx
// app/admin/products/[id]/edit/page.tsx
import { notFound } from 'next/navigation';
import { getAllCategories, getAllFarmers, getProduct } from '@/lib/data';
import ProductForm from '@/components/admin/ProductForm';

export default async function EditProductPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const [product, categories, farmers] = await Promise.all([
    getProduct(id), getAllCategories(), getAllFarmers(),
  ]);
  if (!product) notFound();
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold font-display text-green-950">Sửa: {product.name}</h1>
      <ProductForm initial={product} categories={categories} farmers={farmers} />
    </div>
  );
}
```

- [ ] **Step 5: Smoke manually**

```bash
npm run dev
# /admin/products — see list
# /admin/products/new — create a product, upload image
# edit it, toggle featured, reload /
```

- [ ] **Step 6: Commit**

```bash
git add app/admin/products components/admin/ProductForm.tsx
git commit -m "feat(admin): products list/new/edit pages"
```

---

### Task 29: Categories CRUD

**Files:**
- Create: `app/admin/actions/categories.ts`
- Create: `app/admin/categories/page.tsx`
- Create: `app/admin/categories/new/page.tsx`
- Create: `app/admin/categories/[id]/edit/page.tsx`
- Create: `components/admin/CategoryForm.tsx`

- [ ] **Step 1: Actions**

```ts
// app/admin/actions/categories.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { categories, products } from '@/db/schema';
import { categorySchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';
import type { FormResult } from './products';

export async function upsertCategory(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireAdmin();
  const parsed = categorySchema.safeParse(Object.fromEntries(formData.entries()));
  if (!parsed.success) return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  const c = parsed.data;
  await db.insert(categories).values({ ...c, updatedAt: new Date() })
    .onConflictDoUpdate({
      target: categories.id,
      set: { name: c.name, icon: c.icon, description: c.description, sortOrder: c.sortOrder, updatedAt: new Date() },
    });
  revalidatePath('/');
  revalidatePath('/products');
  return { ok: true };
}

export async function deleteCategory(id: string): Promise<{ ok: boolean; error?: string }> {
  await requireAdmin();
  const using = await db.select({ n: products.id }).from(products).where(eq(products.categoryId, id)).limit(1);
  if (using.length) return { ok: false, error: 'Còn sản phẩm dùng danh mục này' };
  await db.delete(categories).where(eq(categories.id, id));
  revalidatePath('/');
  revalidatePath('/products');
  return { ok: true };
}
```

- [ ] **Step 2: CategoryForm**

```tsx
// components/admin/CategoryForm.tsx
'use client';
import { useActionState } from 'react';
import FormField from './FormField';
import SubmitButton from './SubmitButton';
import { upsertCategory } from '@/app/admin/actions/categories';
import type { FormResult } from '@/app/admin/actions/products';
import type { CategoryRow } from '@/db/schema';

export default function CategoryForm({ initial }: { initial?: CategoryRow }) {
  const [state, action] = useActionState<FormResult, FormData>(upsertCategory, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  return (
    <form action={action} className="bg-white rounded-xl border border-green-100 p-6 max-w-xl space-y-4">
      <FormField label="ID (slug)" error={e.id}>
        <input name="id" defaultValue={initial?.id} readOnly={!!initial} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Tên" error={e.name}>
        <input name="name" defaultValue={initial?.name} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Icon (emoji)" error={e.icon}>
        <input name="icon" defaultValue={initial?.icon} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Mô tả" error={e.description}>
        <input name="description" defaultValue={initial?.description} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Thứ tự hiển thị" error={e.sortOrder}>
        <input name="sortOrder" type="number" defaultValue={initial?.sortOrder ?? 0} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
      <SubmitButton>Lưu</SubmitButton>
    </form>
  );
}
```

- [ ] **Step 3: List/new/edit pages**

```tsx
// app/admin/categories/page.tsx
import Link from 'next/link';
import { asc } from 'drizzle-orm';
import { db } from '@/db/client';
import { categories } from '@/db/schema';
import DataTable from '@/components/admin/DataTable';
import DeleteButton from '@/components/admin/DeleteButton';
import { deleteCategory } from '@/app/admin/actions/categories';

export default async function AdminCategories() {
  const rows = await db.select().from(categories).orderBy(asc(categories.sortOrder), asc(categories.name));
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold font-display text-green-950">Danh mục</h1>
        <Link href="/admin/categories/new" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">+ Mới</Link>
      </div>
      <DataTable
        rows={rows}
        empty="Chưa có danh mục"
        columns={[
          { header: 'Icon', cell: (r) => r.icon },
          { header: 'Tên', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/categories/${r.id}/edit`}>{r.name}</Link> },
          { header: 'Mô tả', cell: (r) => r.description },
          { header: 'Sort', cell: (r) => r.sortOrder },
          { header: '', cell: (r) => <DeleteButton action={async () => { 'use server'; return deleteCategory(r.id); }} /> },
        ]}
      />
    </div>
  );
}
```

```tsx
// app/admin/categories/new/page.tsx
import CategoryForm from '@/components/admin/CategoryForm';
export default function NewCategory() {
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Danh mục mới</h1><CategoryForm /></div>;
}
```

```tsx
// app/admin/categories/[id]/edit/page.tsx
import { notFound } from 'next/navigation';
import { getCategory } from '@/lib/data';
import CategoryForm from '@/components/admin/CategoryForm';

export default async function EditCategory({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const cat = await getCategory(id);
  if (!cat) notFound();
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Sửa: {cat.name}</h1><CategoryForm initial={cat} /></div>;
}
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/categories app/admin/actions/categories.ts components/admin/CategoryForm.tsx
git commit -m "feat(admin): categories CRUD"
```

---

### Task 30: Farmers CRUD

**Files:**
- Create: `app/admin/actions/farmers.ts`
- Create: `app/admin/farmers/page.tsx`
- Create: `app/admin/farmers/new/page.tsx`
- Create: `app/admin/farmers/[id]/edit/page.tsx`
- Create: `components/admin/FarmerForm.tsx`

- [ ] **Step 1: Actions**

```ts
// app/admin/actions/farmers.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { farmers } from '@/db/schema';
import { farmerSchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';
import type { FormResult } from './products';

export async function upsertFarmer(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireAdmin();
  const raw = Object.fromEntries(formData.entries());
  const certRaw = String(raw.certifications ?? '').trim();
  const parsed = farmerSchema.safeParse({
    ...raw,
    certifications: certRaw ? certRaw.split(',').map((s) => s.trim()).filter(Boolean) : [],
  });
  if (!parsed.success) return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  const f = parsed.data;
  await db.insert(farmers).values({ ...f, updatedAt: new Date() })
    .onConflictDoUpdate({
      target: farmers.id,
      set: {
        name: f.name, farm: f.farm, location: f.location, years: f.years,
        specialty: f.specialty, avatar: f.avatar, cover: f.cover, story: f.story,
        certifications: f.certifications, updatedAt: new Date(),
      },
    });
  revalidatePath('/');
  revalidatePath('/farmers');
  revalidatePath(`/farmers/${f.id}`);
  return { ok: true };
}

export async function deleteFarmer(id: string): Promise<{ ok: boolean; error?: string }> {
  await requireAdmin();
  await db.delete(farmers).where(eq(farmers.id, id));
  revalidatePath('/');
  revalidatePath('/farmers');
  return { ok: true };
}
```

- [ ] **Step 2: FarmerForm**

```tsx
// components/admin/FarmerForm.tsx
'use client';
import { useActionState } from 'react';
import FormField from './FormField';
import SubmitButton from './SubmitButton';
import ImageUpload from './ImageUpload';
import { upsertFarmer } from '@/app/admin/actions/farmers';
import type { FormResult } from '@/app/admin/actions/products';
import type { FarmerRow } from '@/db/schema';

export default function FarmerForm({ initial }: { initial?: FarmerRow }) {
  const [state, action] = useActionState<FormResult, FormData>(upsertFarmer, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  return (
    <form action={action} className="grid md:grid-cols-2 gap-6 bg-white rounded-xl border border-green-100 p-6">
      <div className="space-y-4">
        <FormField label="ID (slug)" error={e.id}>
          <input name="id" defaultValue={initial?.id} readOnly={!!initial} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
        </FormField>
        <FormField label="Tên" error={e.name}><input name="name" defaultValue={initial?.name} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        <FormField label="Nông trại" error={e.farm}><input name="farm" defaultValue={initial?.farm} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        <FormField label="Địa điểm" error={e.location}><input name="location" defaultValue={initial?.location} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        <div className="grid grid-cols-2 gap-4">
          <FormField label="Năm kinh nghiệm" error={e.years}><input type="number" name="years" defaultValue={initial?.years} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
          <FormField label="Chuyên môn" error={e.specialty}><input name="specialty" defaultValue={initial?.specialty} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        </div>
        <FormField label="Chứng nhận (phẩy ngăn cách)"><input name="certifications" defaultValue={initial?.certifications?.join(', ')} className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
      </div>
      <div className="space-y-4">
        <ImageUpload name="avatar" defaultValue={initial?.avatar} label="Avatar" />
        <ImageUpload name="cover" defaultValue={initial?.cover} label="Ảnh bìa" />
        <FormField label="Câu chuyện" error={e.story}><textarea name="story" defaultValue={initial?.story} required rows={6} className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
        <SubmitButton>Lưu</SubmitButton>
      </div>
    </form>
  );
}
```

- [ ] **Step 3: List/new/edit pages (analogous to categories)**

```tsx
// app/admin/farmers/page.tsx
import Link from 'next/link';
import { asc } from 'drizzle-orm';
import { db } from '@/db/client';
import { farmers } from '@/db/schema';
import DataTable from '@/components/admin/DataTable';
import DeleteButton from '@/components/admin/DeleteButton';
import { deleteFarmer } from '@/app/admin/actions/farmers';

export default async function AdminFarmers() {
  const rows = await db.select().from(farmers).orderBy(asc(farmers.name));
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold font-display text-green-950">Nông dân</h1>
        <Link href="/admin/farmers/new" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">+ Mới</Link>
      </div>
      <DataTable rows={rows} empty="Chưa có nông dân" columns={[
        { header: 'Tên', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/farmers/${r.id}/edit`}>{r.name}</Link> },
        { header: 'Nông trại', cell: (r) => r.farm },
        { header: 'Địa điểm', cell: (r) => r.location },
        { header: 'Năm', cell: (r) => r.years },
        { header: '', cell: (r) => <DeleteButton action={async () => { 'use server'; return deleteFarmer(r.id); }} /> },
      ]} />
    </div>
  );
}
```

```tsx
// app/admin/farmers/new/page.tsx
import FarmerForm from '@/components/admin/FarmerForm';
export default function NewFarmer() {
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Nông dân mới</h1><FarmerForm /></div>;
}
```

```tsx
// app/admin/farmers/[id]/edit/page.tsx
import { notFound } from 'next/navigation';
import { getFarmer } from '@/lib/data';
import FarmerForm from '@/components/admin/FarmerForm';

export default async function EditFarmer({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const f = await getFarmer(id);
  if (!f) notFound();
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Sửa: {f.name}</h1><FarmerForm initial={f} /></div>;
}
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/farmers app/admin/actions/farmers.ts components/admin/FarmerForm.tsx
git commit -m "feat(admin): farmers CRUD"
```

---

### Task 31: Testimonials CRUD

**Files:**
- Create: `app/admin/actions/testimonials.ts`
- Create: `app/admin/testimonials/page.tsx`
- Create: `app/admin/testimonials/new/page.tsx`
- Create: `app/admin/testimonials/[id]/edit/page.tsx`
- Create: `components/admin/TestimonialForm.tsx`

- [ ] **Step 1: Actions**

```ts
// app/admin/actions/testimonials.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { testimonials } from '@/db/schema';
import { testimonialSchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';
import type { FormResult } from './products';

export async function upsertTestimonial(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireAdmin();
  const parsed = testimonialSchema.safeParse(Object.fromEntries(formData.entries()));
  if (!parsed.success) return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  const t = parsed.data;
  if (t.id) {
    await db.update(testimonials).set({
      name: t.name, role: t.role, avatar: t.avatar, content: t.content, sortOrder: t.sortOrder,
    }).where(eq(testimonials.id, t.id));
  } else {
    await db.insert(testimonials).values({
      name: t.name, role: t.role, avatar: t.avatar, content: t.content, sortOrder: t.sortOrder,
    });
  }
  revalidatePath('/');
  return { ok: true };
}

export async function deleteTestimonial(id: number): Promise<{ ok: boolean; error?: string }> {
  await requireAdmin();
  await db.delete(testimonials).where(eq(testimonials.id, id));
  revalidatePath('/');
  return { ok: true };
}
```

- [ ] **Step 2: Form**

```tsx
// components/admin/TestimonialForm.tsx
'use client';
import { useActionState } from 'react';
import FormField from './FormField';
import SubmitButton from './SubmitButton';
import ImageUpload from './ImageUpload';
import { upsertTestimonial } from '@/app/admin/actions/testimonials';
import type { FormResult } from '@/app/admin/actions/products';
import type { TestimonialRow } from '@/db/schema';

export default function TestimonialForm({ initial }: { initial?: TestimonialRow }) {
  const [state, action] = useActionState<FormResult, FormData>(upsertTestimonial, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  return (
    <form action={action} className="grid md:grid-cols-2 gap-6 bg-white rounded-xl border border-green-100 p-6">
      <input type="hidden" name="id" defaultValue={initial?.id} />
      <div className="space-y-4">
        <FormField label="Tên" error={e.name}><input name="name" defaultValue={initial?.name} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        <FormField label="Vai trò" error={e.role}><input name="role" defaultValue={initial?.role} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        <FormField label="Thứ tự" error={e.sortOrder}><input name="sortOrder" type="number" defaultValue={initial?.sortOrder ?? 0} className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
      </div>
      <div className="space-y-4">
        <ImageUpload name="avatar" defaultValue={initial?.avatar} label="Avatar" />
        <FormField label="Nội dung" error={e.content}><textarea name="content" defaultValue={initial?.content} required rows={5} className="w-full border border-green-200 rounded px-3 py-2 text-sm" /></FormField>
        {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
        <SubmitButton>Lưu</SubmitButton>
      </div>
    </form>
  );
}
```

- [ ] **Step 3: Pages**

```tsx
// app/admin/testimonials/page.tsx
import Link from 'next/link';
import { asc } from 'drizzle-orm';
import { db } from '@/db/client';
import { testimonials } from '@/db/schema';
import DataTable from '@/components/admin/DataTable';
import DeleteButton from '@/components/admin/DeleteButton';
import { deleteTestimonial } from '@/app/admin/actions/testimonials';

export default async function AdminTestimonials() {
  const rows = await db.select().from(testimonials).orderBy(asc(testimonials.sortOrder), asc(testimonials.id));
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold font-display text-green-950">Cảm nhận</h1>
        <Link href="/admin/testimonials/new" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">+ Mới</Link>
      </div>
      <DataTable rows={rows} empty="Chưa có cảm nhận" columns={[
        { header: 'Tên', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/testimonials/${r.id}/edit`}>{r.name}</Link> },
        { header: 'Vai trò', cell: (r) => r.role },
        { header: 'Nội dung', cell: (r) => <span className="line-clamp-1">{r.content}</span> },
        { header: 'Sort', cell: (r) => r.sortOrder },
        { header: '', cell: (r) => <DeleteButton action={async () => { 'use server'; return deleteTestimonial(r.id); }} /> },
      ]} />
    </div>
  );
}
```

```tsx
// app/admin/testimonials/new/page.tsx
import TestimonialForm from '@/components/admin/TestimonialForm';
export default function NewTestimonial() {
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Cảm nhận mới</h1><TestimonialForm /></div>;
}
```

```tsx
// app/admin/testimonials/[id]/edit/page.tsx
import { notFound } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { testimonials } from '@/db/schema';
import TestimonialForm from '@/components/admin/TestimonialForm';

export default async function EditTestimonial({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const rows = await db.select().from(testimonials).where(eq(testimonials.id, Number(id))).limit(1);
  if (!rows[0]) notFound();
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Sửa cảm nhận</h1><TestimonialForm initial={rows[0]} /></div>;
}
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/testimonials app/admin/actions/testimonials.ts components/admin/TestimonialForm.tsx
git commit -m "feat(admin): testimonials CRUD"
```

---

### Task 32: FAQ CRUD

**Files:**
- Create: `app/admin/actions/faq.ts`
- Create: `app/admin/faq/page.tsx`
- Create: `app/admin/faq/new/page.tsx`
- Create: `app/admin/faq/[id]/edit/page.tsx`
- Create: `components/admin/FaqForm.tsx`

- [ ] **Step 1: Actions**

```ts
// app/admin/actions/faq.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { faqItems } from '@/db/schema';
import { faqSchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';
import type { FormResult } from './products';

export async function upsertFaq(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireAdmin();
  const parsed = faqSchema.safeParse(Object.fromEntries(formData.entries()));
  if (!parsed.success) return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  const f = parsed.data;
  if (f.id) {
    await db.update(faqItems).set({
      question: f.question, answer: f.answer, sortOrder: f.sortOrder,
    }).where(eq(faqItems.id, f.id));
  } else {
    await db.insert(faqItems).values({ question: f.question, answer: f.answer, sortOrder: f.sortOrder });
  }
  revalidatePath('/');
  return { ok: true };
}

export async function deleteFaq(id: number): Promise<{ ok: boolean; error?: string }> {
  await requireAdmin();
  await db.delete(faqItems).where(eq(faqItems.id, id));
  revalidatePath('/');
  return { ok: true };
}
```

- [ ] **Step 2: FaqForm**

```tsx
// components/admin/FaqForm.tsx
'use client';
import { useActionState } from 'react';
import FormField from './FormField';
import SubmitButton from './SubmitButton';
import { upsertFaq } from '@/app/admin/actions/faq';
import type { FormResult } from '@/app/admin/actions/products';
import type { FaqRow } from '@/db/schema';

export default function FaqForm({ initial }: { initial?: FaqRow }) {
  const [state, action] = useActionState<FormResult, FormData>(upsertFaq, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  return (
    <form action={action} className="bg-white rounded-xl border border-green-100 p-6 max-w-2xl space-y-4">
      <input type="hidden" name="id" defaultValue={initial?.id} />
      <FormField label="Câu hỏi" error={e.question}>
        <input name="question" defaultValue={initial?.question} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Trả lời" error={e.answer}>
        <textarea name="answer" defaultValue={initial?.answer} required rows={5} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Thứ tự" error={e.sortOrder}>
        <input name="sortOrder" type="number" defaultValue={initial?.sortOrder ?? 0} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
      <SubmitButton>Lưu</SubmitButton>
    </form>
  );
}
```

- [ ] **Step 3: List/new/edit pages**

```tsx
// app/admin/faq/page.tsx
import Link from 'next/link';
import { asc } from 'drizzle-orm';
import { db } from '@/db/client';
import { faqItems } from '@/db/schema';
import DataTable from '@/components/admin/DataTable';
import DeleteButton from '@/components/admin/DeleteButton';
import { deleteFaq } from '@/app/admin/actions/faq';

export default async function AdminFaq() {
  const rows = await db.select().from(faqItems).orderBy(asc(faqItems.sortOrder), asc(faqItems.id));
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold font-display text-green-950">FAQ</h1>
        <Link href="/admin/faq/new" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">+ Mới</Link>
      </div>
      <DataTable rows={rows} empty="Chưa có FAQ" columns={[
        { header: 'Câu hỏi', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/faq/${r.id}/edit`}>{r.question}</Link> },
        { header: 'Trả lời', cell: (r) => <span className="line-clamp-1">{r.answer}</span> },
        { header: 'Sort', cell: (r) => r.sortOrder },
        { header: '', cell: (r) => <DeleteButton action={async () => { 'use server'; return deleteFaq(r.id); }} /> },
      ]} />
    </div>
  );
}
```

```tsx
// app/admin/faq/new/page.tsx
import FaqForm from '@/components/admin/FaqForm';
export default function NewFaq() {
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">FAQ mới</h1><FaqForm /></div>;
}
```

```tsx
// app/admin/faq/[id]/edit/page.tsx
import { notFound } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { faqItems } from '@/db/schema';
import FaqForm from '@/components/admin/FaqForm';

export default async function EditFaq({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const rows = await db.select().from(faqItems).where(eq(faqItems.id, Number(id))).limit(1);
  if (!rows[0]) notFound();
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Sửa FAQ</h1><FaqForm initial={rows[0]} /></div>;
}
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/faq app/admin/actions/faq.ts components/admin/FaqForm.tsx
git commit -m "feat(admin): faq CRUD"
```

---

### Task 33: Site settings (single row)

**Files:**
- Create: `app/admin/actions/settings.ts`
- Create: `app/admin/settings/page.tsx`

- [ ] **Step 1: Action**

```ts
// app/admin/actions/settings.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { siteInfo } from '@/db/schema';
import { siteInfoSchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';
import type { FormResult } from './products';

export async function saveSettings(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireAdmin();
  const parsed = siteInfoSchema.safeParse(Object.fromEntries(formData.entries()));
  if (!parsed.success) return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  await db.update(siteInfo).set(parsed.data).where(eq(siteInfo.id, 1));
  revalidatePath('/', 'layout');
  return { ok: true };
}
```

- [ ] **Step 2: Settings page**

```tsx
// app/admin/settings/page.tsx
'use client';
import { useActionState } from 'react';
import FormField from '@/components/admin/FormField';
import SubmitButton from '@/components/admin/SubmitButton';
import { saveSettings } from '@/app/admin/actions/settings';
import type { FormResult } from '@/app/admin/actions/products';
import type { SiteInfoRow } from '@/db/schema';

export default function SettingsClient({ initial }: { initial: SiteInfoRow }) {
  const [state, action] = useActionState<FormResult, FormData>(saveSettings, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  const field = (name: keyof SiteInfoRow, label: string, type: 'text' | 'textarea' = 'text') => (
    <FormField key={name as string} label={label} name={name as string} error={(e as any)[name]}>
      {type === 'textarea' ? (
        <textarea name={name as string} defaultValue={(initial as any)[name] ?? ''} rows={3} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      ) : (
        <input name={name as string} defaultValue={(initial as any)[name] ?? ''} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      )}
    </FormField>
  );
  return (
    <form action={action} className="bg-white rounded-xl border border-green-100 p-6 grid md:grid-cols-2 gap-4 max-w-4xl">
      {field('name', 'Tên thương hiệu')}
      {field('shortName', 'Tên ngắn')}
      {field('tagline', 'Tagline')}
      {field('description', 'Mô tả', 'textarea')}
      {field('address', 'Địa chỉ')}
      {field('phone', 'Điện thoại')}
      {field('email', 'Email')}
      {field('hours', 'Giờ mở cửa')}
      {field('statFarmers', 'Số liệu: Nông dân')}
      {field('statProducts', 'Số liệu: Sản phẩm')}
      {field('statCustomers', 'Số liệu: Khách hàng')}
      {field('statYears', 'Số liệu: Năm kinh nghiệm')}
      <div className="md:col-span-2 flex items-center gap-4">
        {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
        <SubmitButton>Lưu cài đặt</SubmitButton>
      </div>
    </form>
  );
}
```

Wrap with a server component to fetch initial:

```tsx
// app/admin/settings/page.tsx (server wrapper)
import { getSiteInfo } from '@/lib/data';
import SettingsClient from './settings-client';

export default async function SettingsPage() {
  const info = await getSiteInfo();
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold font-display text-green-950">Cài đặt site</h1>
      <SettingsClient initial={info} />
    </div>
  );
}
```

Rename the client component file accordingly (`app/admin/settings/settings-client.tsx`).

- [ ] **Step 3: Commit**

```bash
git add app/admin/settings app/admin/actions/settings.ts
git commit -m "feat(admin): site settings page"
```

---

### Task 34: Orders list + detail + status change

**Files:**
- Create: `app/admin/actions/orders.ts`
- Create: `app/admin/orders/page.tsx`
- Create: `app/admin/orders/[id]/page.tsx`

- [ ] **Step 1: Action**

```ts
// app/admin/actions/orders.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { orders } from '@/db/schema';
import { orderStatusSchema } from '@/lib/validators';
import { requireAdmin } from '@/lib/session';

export async function updateOrderStatus(id: string, status: string): Promise<{ ok: boolean; error?: string }> {
  await requireAdmin();
  const parsed = orderStatusSchema.safeParse(status);
  if (!parsed.success) return { ok: false, error: 'Trạng thái không hợp lệ' };
  await db.update(orders).set({ status: parsed.data, updatedAt: new Date() }).where(eq(orders.id, id));
  revalidatePath('/admin/orders');
  revalidatePath(`/admin/orders/${id}`);
  return { ok: true };
}
```

- [ ] **Step 2: Orders list**

```tsx
// app/admin/orders/page.tsx
import Link from 'next/link';
import { desc, eq, and, sql } from 'drizzle-orm';
import { db } from '@/db/client';
import { orders } from '@/db/schema';
import { formatPrice } from '@/lib/data';
import DataTable from '@/components/admin/DataTable';
import Pagination from '@/components/admin/Pagination';

const PAGE_SIZE = 20;
const STATUS = ['pending', 'preparing', 'delivering', 'delivered', 'cancelled'] as const;

export default async function AdminOrders({
  searchParams,
}: { searchParams: Promise<{ status?: string; page?: string }> }) {
  const { status, page: pageParam } = await searchParams;
  const page = Math.max(1, Number(pageParam) || 1);
  const where = status && STATUS.includes(status as any) ? eq(orders.status, status as any) : undefined;
  const [rows, totalRows] = await Promise.all([
    db.select().from(orders).where(where as any).orderBy(desc(orders.createdAt))
      .limit(PAGE_SIZE).offset((page - 1) * PAGE_SIZE),
    db.select({ n: sql<number>`count(*)::int` }).from(orders).where(where as any),
  ]);
  const total = totalRows[0]?.n ?? 0;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold font-display text-green-950">Đơn hàng</h1>
      <div className="flex gap-2 text-sm">
        <Link href="/admin/orders" className={`px-3 py-1 rounded border border-green-200 ${!status ? 'bg-green-700 text-white' : ''}`}>Tất cả</Link>
        {STATUS.map((s) => (
          <Link key={s} href={`?status=${s}`} className={`px-3 py-1 rounded border border-green-200 ${status === s ? 'bg-green-700 text-white' : ''}`}>{s}</Link>
        ))}
      </div>
      <DataTable rows={rows} empty="Chưa có đơn" columns={[
        { header: 'Mã', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/orders/${r.id}`}>{r.id}</Link> },
        { header: 'Khách', cell: (r) => r.customerName },
        { header: 'SĐT', cell: (r) => r.phone },
        { header: 'Tổng', cell: (r) => formatPrice(r.total) },
        { header: 'Trạng thái', cell: (r) => r.status },
        { header: 'Ngày', cell: (r) => new Date(r.createdAt).toLocaleString('vi-VN') },
      ]} />
      <Pagination page={page} total={total} pageSize={PAGE_SIZE} params={{ status }} />
    </div>
  );
}
```

- [ ] **Step 3: Order detail + status form**

```tsx
// app/admin/orders/[id]/page.tsx
import { notFound } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { orders, orderItems } from '@/db/schema';
import { formatPrice } from '@/lib/data';
import { updateOrderStatus } from '@/app/admin/actions/orders';

const STATUS = ['pending', 'preparing', 'delivering', 'delivered', 'cancelled'] as const;

export default async function AdminOrderDetail({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const [o] = await db.select().from(orders).where(eq(orders.id, id)).limit(1);
  if (!o) notFound();
  const items = await db.select().from(orderItems).where(eq(orderItems.orderId, id));

  async function changeStatus(formData: FormData) {
    'use server';
    await updateOrderStatus(id, String(formData.get('status')));
  }

  return (
    <div className="space-y-5 max-w-3xl">
      <h1 className="text-2xl font-bold font-display text-green-950">Đơn {o.id}</h1>
      <div className="bg-white rounded-xl border border-green-100 p-6 grid md:grid-cols-3 gap-4 text-sm">
        <div><div className="text-xs text-green-900/60">Khách</div><div className="font-semibold">{o.customerName}</div></div>
        <div><div className="text-xs text-green-900/60">SĐT</div><div>{o.phone}</div></div>
        <div><div className="text-xs text-green-900/60">Khung giờ</div><div>{o.deliverySlot}</div></div>
        <div className="md:col-span-3"><div className="text-xs text-green-900/60">Địa chỉ</div><div>{o.address}</div></div>
        {o.note && <div className="md:col-span-3"><div className="text-xs text-green-900/60">Ghi chú</div><div>{o.note}</div></div>}
      </div>

      <div className="bg-white rounded-xl border border-green-100 p-4">
        <ul className="divide-y divide-green-50">
          {items.map((i) => (
            <li key={i.id} className="flex items-center gap-3 py-2 text-sm">
              {/* eslint-disable-next-line @next/next/no-img-element */}
              <img src={i.image} alt="" className="w-10 h-10 rounded object-cover" />
              <div className="flex-1"><div className="font-semibold">{i.name}</div><div className="text-xs text-green-900/60">{i.qty} × {i.unit}</div></div>
              <div className="font-semibold">{formatPrice(i.price * i.qty)}</div>
            </li>
          ))}
        </ul>
        <div className="border-t border-green-100 pt-2 text-right font-bold text-lg">{formatPrice(o.total)}</div>
      </div>

      <form action={changeStatus} className="bg-white rounded-xl border border-green-100 p-4 flex items-center gap-3">
        <label className="text-sm font-semibold">Trạng thái:</label>
        <select name="status" defaultValue={o.status} className="border border-green-200 rounded px-3 py-1.5 text-sm">
          {STATUS.map((s) => <option key={s} value={s}>{s}</option>)}
        </select>
        <button type="submit" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">Cập nhật</button>
      </form>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/orders app/admin/actions/orders.ts
git commit -m "feat(admin): orders list + detail with status update"
```

---

### Task 35: Users CRUD (admin-only)

**Files:**
- Create: `app/admin/actions/users.ts`
- Create: `app/admin/users/layout.tsx` (enforce admin-only)
- Create: `app/admin/users/page.tsx`
- Create: `app/admin/users/new/page.tsx`
- Create: `app/admin/users/[id]/edit/page.tsx`
- Create: `components/admin/UserForm.tsx`

- [ ] **Step 1: Layout enforcing admin**

```tsx
// app/admin/users/layout.tsx
import { requireRole } from '@/lib/session';

export default async function UsersLayout({ children }: { children: React.ReactNode }) {
  await requireRole('admin');
  return <>{children}</>;
}
```

- [ ] **Step 2: Actions**

```ts
// app/admin/actions/users.ts
'use server';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { users } from '@/db/schema';
import { userCreateSchema, userUpdateSchema } from '@/lib/validators';
import { requireRole } from '@/lib/session';
import { hashPassword } from '@/lib/auth';
import type { FormResult } from './products';

export async function upsertUser(_prev: FormResult, formData: FormData): Promise<FormResult> {
  await requireRole('admin');
  const idRaw = formData.get('id');
  const isEdit = !!idRaw;
  const raw = Object.fromEntries(formData.entries());
  const schema = isEdit ? userUpdateSchema : userCreateSchema;
  const parsed = schema.safeParse(raw);
  if (!parsed.success) return { ok: false, fieldErrors: parsed.error.flatten().fieldErrors };
  const data = parsed.data;

  if (isEdit) {
    const updates: Record<string, unknown> = { updatedAt: new Date() };
    if (data.email) updates.email = data.email;
    if (data.name) updates.name = data.name;
    if (data.role) updates.role = data.role;
    if (data.password) updates.passwordHash = await hashPassword(data.password);
    await db.update(users).set(updates).where(eq(users.id, String(idRaw)));
  } else {
    await db.insert(users).values({
      email: data.email!, name: data.name!, role: data.role!,
      passwordHash: await hashPassword(data.password!),
    });
  }
  revalidatePath('/admin/users');
  return { ok: true };
}

export async function deleteUser(id: string): Promise<{ ok: boolean; error?: string }> {
  const me = await requireRole('admin');
  if (me.id === id) return { ok: false, error: 'Không thể tự xoá tài khoản đang dùng' };
  await db.delete(users).where(eq(users.id, id));
  revalidatePath('/admin/users');
  return { ok: true };
}
```

- [ ] **Step 3: UserForm**

```tsx
// components/admin/UserForm.tsx
'use client';
import { useActionState } from 'react';
import FormField from './FormField';
import SubmitButton from './SubmitButton';
import { upsertUser } from '@/app/admin/actions/users';
import type { FormResult } from '@/app/admin/actions/products';
import type { UserRow } from '@/db/schema';

export default function UserForm({ initial }: { initial?: UserRow }) {
  const [state, action] = useActionState<FormResult, FormData>(upsertUser, null);
  const e = (state && !state.ok && state.fieldErrors) || {};
  return (
    <form action={action} className="bg-white rounded-xl border border-green-100 p-6 max-w-xl space-y-4">
      {initial && <input type="hidden" name="id" defaultValue={initial.id} />}
      <FormField label="Email" error={e.email}>
        <input name="email" type="email" defaultValue={initial?.email} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Tên" error={e.name}>
        <input name="name" defaultValue={initial?.name} required className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      <FormField label="Vai trò" error={e.role}>
        <select name="role" defaultValue={initial?.role ?? 'staff'} className="w-full border border-green-200 rounded px-3 py-2 text-sm">
          <option value="admin">admin</option>
          <option value="staff">staff</option>
        </select>
      </FormField>
      <FormField label={initial ? 'Mật khẩu mới (để trống nếu không đổi)' : 'Mật khẩu'} error={e.password}>
        <input name="password" type="password" minLength={initial ? 0 : 8} {...(initial ? {} : { required: true })} className="w-full border border-green-200 rounded px-3 py-2 text-sm" />
      </FormField>
      {state && state.ok && <p className="text-sm text-green-700">Đã lưu ✓</p>}
      <SubmitButton>Lưu</SubmitButton>
    </form>
  );
}
```

- [ ] **Step 4: Pages**

```tsx
// app/admin/users/page.tsx
import Link from 'next/link';
import { asc } from 'drizzle-orm';
import { db } from '@/db/client';
import { users } from '@/db/schema';
import DataTable from '@/components/admin/DataTable';
import DeleteButton from '@/components/admin/DeleteButton';
import { deleteUser } from '@/app/admin/actions/users';

export default async function AdminUsers() {
  const rows = await db.select().from(users).orderBy(asc(users.email));
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold font-display text-green-950">Tài khoản</h1>
        <Link href="/admin/users/new" className="bg-green-700 text-white font-semibold px-4 py-1.5 rounded text-sm">+ Mới</Link>
      </div>
      <DataTable rows={rows} empty="Chưa có người dùng" columns={[
        { header: 'Email', cell: (r) => <Link className="text-green-800 font-semibold" href={`/admin/users/${r.id}/edit`}>{r.email}</Link> },
        { header: 'Tên', cell: (r) => r.name },
        { header: 'Vai trò', cell: (r) => r.role },
        { header: '', cell: (r) => <DeleteButton action={async () => { 'use server'; return deleteUser(r.id); }} /> },
      ]} />
    </div>
  );
}
```

```tsx
// app/admin/users/new/page.tsx
import UserForm from '@/components/admin/UserForm';
export default function NewUser() {
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Người dùng mới</h1><UserForm /></div>;
}
```

```tsx
// app/admin/users/[id]/edit/page.tsx
import { notFound } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { db } from '@/db/client';
import { users } from '@/db/schema';
import UserForm from '@/components/admin/UserForm';

export default async function EditUser({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const rows = await db.select().from(users).where(eq(users.id, id)).limit(1);
  if (!rows[0]) notFound();
  return <div className="space-y-4"><h1 className="text-2xl font-bold font-display text-green-950">Sửa: {rows[0].email}</h1><UserForm initial={rows[0]} /></div>;
}
```

- [ ] **Step 5: Commit**

```bash
git add app/admin/users app/admin/actions/users.ts components/admin/UserForm.tsx
git commit -m "feat(admin): users CRUD (admin-only)"
```

---

## Phase 8: Final verification

### Task 36: Manual smoke checklist + remove JSON

**Files:**
- Create: `docs/superpowers/specs/checklists/cms-smoke.md`
- Modify: `data/` (optional — backup, keep or delete)

- [ ] **Step 1: Write smoke checklist**

```md
# CMS Smoke Checklist

Run before declaring done.

- [ ] `docker compose up -d postgres`
- [ ] `npm run db:migrate && npm run db:seed && npm run db:seed-admin`
- [ ] `npm run dev`
- [ ] Visit `/` — hero + products + farmers + testimonials + FAQ render from DB
- [ ] Visit `/products`, filter by category, click product
- [ ] Visit `/farmers`, click farmer, see their products
- [ ] Add to cart, checkout, place order → land on `/orders?new=NTX-XXX` showing order
- [ ] Visit `/orders` in another tab — same order visible via `ntx_my_orders` cookie
- [ ] Visit `/admin` without login → redirected to `/admin/login`
- [ ] Log in with `ADMIN_EMAIL`/`ADMIN_PASSWORD` → dashboard with stats
- [ ] `/admin/products`: create a new product, upload an image, toggle featured, save
- [ ] Go to `/` → new product appears in featured section
- [ ] `/admin/orders`: see the placed order; change status to `preparing`; visit `/orders` in the other tab → status updated
- [ ] `/admin/users`: create a `staff` user; log out; log in as staff; `/admin/users` should be blocked
- [ ] Log out → back to login
- [ ] `npm run build` succeeds
- [ ] `npx vitest run` all tests green
```

- [ ] **Step 2: Run full build + tests**

```bash
npm run build
npx vitest run
```
Expected: both succeed.

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/checklists/cms-smoke.md
git commit -m "docs: cms smoke checklist"
```

- [ ] **Step 4: Execute the checklist by hand**

Do not skip. Work through every box.

---

## Spec Coverage Check (completed during plan review)

| Spec section | Tasks that implement it |
| --- | --- |
| §3 directory structure | T11, T22, T24 (proxy / layouts), T28–T35 (admin) |
| §4.1 tables | T4 |
| §4.2 indexes | Deferred — add in a follow-up migration if performance warrants |
| §4.3 seed | T6, T8 |
| §5 auth (cookie, proxy, flow, signIn, logout, roles) | T7, T9, T22–T24, T35 (roles) |
| §6.1 shell | T24 |
| §6.2 list pattern | T25, T28 |
| §6.3 form pattern | T25, T28 |
| §6.4 dashboard / orders detail / settings | T26, T33, T34 |
| §6.5 upload | T19, T20, T21 |
| §7.1 lib/data.ts async | T12 |
| §7.2 checkout + orders cookie | T15, T16, T17, T18 |
| §7.3 migration checklist | Phases 3–4 sequencing |
| §8 testing (auth, uploads, place-order, products integration) | T7, T16, T19, T27 |
| §9 env vars | T2 |
| §10 docker compose | T2 |
| §11 dependencies | T1 |

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-22-cms-admin.md`.

Two execution options:
1. **Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** — execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
