apiVersion: v1
kind: Service
metadata:
  name: {{ include "vacu.fullname" . }}
  labels:
    {{- include "vacu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "vacu.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
