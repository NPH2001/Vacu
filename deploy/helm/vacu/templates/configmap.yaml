apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vacu.fullname" . }}-config
  labels:
    {{- include "vacu.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
